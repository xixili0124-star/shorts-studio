# 고정 버전 엔진과 라이선스의 원문 바이트·SHA-256을 운영체제마다 동일하게 보존합니다.
public/vendor/transformers/** -text
public/vendor/onnxruntime-web/** -text
public/vendor/supertonic/** -text
# 출처 검증에 사용하는 라이선스·메타데이터의 원문 바이트를 보존합니다.
# 외부 원문의 행 끝 공백은 원작자 파일과 해시를 일치시키기 위해 유지합니다.
public/licenses/google-fonts/*/OFL.txt -text -whitespace
public/licenses/google-fonts/*/METADATA.pb -text -whitespace
public/sounds/licenses/*.txt -text
# 고정 해시로 검증하는 공급자 파일은 줄바꿈·공백을 포함해 원본 바이트를 보존합니다.
public/vendor/mediapipe/** -text -whitespace
