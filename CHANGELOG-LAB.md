# 실험판 변경 기록

기준 저장소: `xixili0124-star/shorts-studio`

기준 커밋: `9eb4ef137222d393d247cf79f6f2a1b4b7713368`

작업 방식: 기준 스냅샷을 별도 폴더로 복사했습니다. 로컬 실험판 폴더에는 `.git`이 없습니다. 사용자 요청에 따라 `codex/studio-lab` 공유 브랜치와 검토용 Draft PR을 만들며, 운영 `main`과 원본 참조 clone은 보존합니다.

공유 준비 중 `main`의 새 커밋 `ee8e32563022f20dad147bd23d739ec993e32101`(TTS 추가)을 확인했습니다. 이 시제품은 기존 기준 커밋에서 분기하며, 새 TTS 작업을 자동 병합하거나 덮어쓰지 않습니다. 정식 통합 전에 관련 코어 변경을 조정해야 합니다.

## 2026-08-28 · VoxCPM2 PC 기본 엔진

- PC 내 목소리 엔진에 VoxCPM2 2B를 추가했습니다. Supertonic 2·자동자막·타임라인과 기존 GPT-SoVITS를 유지합니다. 새 설치 기본은 Vox이며 기존 설정은 설치 완료 전까지 GPT로 인식합니다.
- Vox 전용 Python 환경과 고정된 모델 약 5GB를 별도 설치합니다. 기존 설정·키를 엔진별로 보관하고 참고 녹음은 같은 비공개 폴더에서 공유합니다. `--provider gpt-sovits` / `--provider voxcpm2`로 실행 엔진을 선택합니다.
- 모델의 중국어/영어 전용 정규화·denoiser·원격 tokenizer 코드를 사용하지 않습니다. 스포츠 점수 `4대 2`를 `사 대 이`로 처리하고 일반 개수는 유지합니다. 원고를 짧게 나누고 참고 캐시를 요청당 한 번 만들며, 재시도 소진·출력 제한 도달 시 부분 WAV를 성공으로 반환하지 않습니다.
- 실제 48kHz 모노 WAV를 반환하고 0.75~1.5배속은 생성 후 음높이를 유지해 처리합니다. 동시 추론을 직렬화하고 응답이 불확실하면 재시작 전 참고 삭제를 막습니다.
- 엔진 인증 서명에 provider와 모델을 포함합니다. 기존 GPT 서명은 GPT에만 허용하며 Vox로 오인하지 않습니다. 개인 키·경로·참고 음성을 공개 사이트나 로그로 보내지 않습니다.
- Node **135개**, Python **36개** 검사 통과. RTX 3060에서 한국어, 숫자 점수, 한글 점수, 1.2배속, 두 청크 원고의 실제 WAV 생성 5개를 검증했습니다. 숫자/한글 점수 출력 일치, 속도별 길이, 임시 참고 삭제와 기존 파일 보존을 확인했습니다. 사용자 목소리 유사도·주관적 자연스러움·브라우저 전체 조작 검사는 별도입니다.

## 이전 업데이트 · 선택형 PC 내 목소리 TTS

- Supertonic 2 기본 TTS·Whisper Tiny 자동자막·기존 편집을 유지하며 AI 음성 패널에 PC 확장 모드를 추가했습니다. 공개/모바일 주소에는 설치 안내를 보여주고 PC 엔진을 탐색하지 않습니다.
- GPT-SoVITS v2ProPlus의 3~10초 참고 음성 생성 방식을 연결했습니다. 마이크·파일 등록, 읽은 문장, 사용 동의, 등록 목록·삭제, 새 원고·속도, 미리듣기·WAV 저장·보이스 트랙 추가를 편집기에서 처리합니다. 미세 학습은 포함하지 않습니다.
- 참고 음성·문장·엔진 키·모델은 공개 경로 밖에 보관합니다. 등록창 드롭/붙여넣기를 소재함 가져오기와 분리하고 생성된 음성만 .shorts에 담습니다. 연결 확인 중 원고 textarea를 재생성하지 않아 입력과 한글 조합을 유지합니다.
- 동일 출처·Host·사용 동의, 고정 upstream, 요청·응답 크기·PCM 검증, 임의 경로·프록시·리다이렉트 차단을 추가했습니다. 키 기반 challenge 응답으로 보호 없는 원본 API를 잘못 재사용하지 않습니다. 관리자 API 제거·추론 시 외부 Python 소켓 차단·원고 로그 폐기를 적용했습니다.
- 취소 이후 늦은 결과를 버리고 실제 엔진 작업이 끝날 때까지 잠금을 유지합니다. 저장·삭제 응답은 창 닫기와 분리하며, 삭제 실패 시 참고 파일을 숨기지 않고 재시도를 제공합니다.
- Windows용 격리 설치·실행기와 앱 안의 PC 안내를 추가했습니다. 코드·모델 revision과 모델 SHA-256, 한국어/영어 발음 리소스를 고정했습니다. 전역 Python·원본 엔진 소스는 수정하지 않습니다. 새 리소스가 준비된 후 실행 설정을 게시합니다.
- Windows 실행기는 새 자식을 띄우기 전에 전용 Job에 들어가며, 종료할 때 venv 중간 실행기와 실제 엔진 자식까지 정리합니다. 다른 실행기에서 재사용한 엔진은 종료하지 않습니다.
- Node **135개**, Python **25개** 검사 통과. RTX 3060에서 실제 한국어/숫자·영문 혼용 WAV 두 개를 생성했습니다. 참고는 기존 합성 음성이며 사용자 목소리·주관적 품질·모바일 실기·전체 브라우저 UI/MP4 검수는 미실행입니다.

## 이전 업데이트 · 다중 선택·설정 복사·모니터 직접 편집

- 빈 공간에서 드래그하는 사각 선택, Shift/Ctrl 추가·반전 선택과 전체 선택을 추가했습니다. 일괄 속성·삭제·분할·복제·같은 행 유지 묶음 이동에 연결했습니다.
- 각 클립 상단과 타임라인/속성 패널에 설정 복사·붙여넣기를 추가했습니다. 내용·소재·시각·길이·모자이크는 유지하고 호환되는 설정만 깊은 복사합니다.
- 영상·그래픽·자막·오디오·보이스 이름과 행별 ＋를 복원했습니다. 시각 행은 바로 위, 소리 행은 바로 아래에 추가합니다. 기존 ID·쌓임 순서·시각·오디오 용도와 버전 4 저장을 보존합니다.
- 모니터 선택 테두리 위 변형/자르기 스위치, 8방향 손잡이·회전·중앙 정렬·중앙 스냅을 추가했습니다. 회전·반전·비균일 확대 상태에서도 원본의 crop 경계를 조절하고 반대편 resize 경계를 고정합니다.
- 단일 작업/일괄 작업 모두 한 번의 undo를 사용합니다. 취소·포인터 캡처 상실·다른 포인터·창 포커스 상실을 처리하고 복제/분할 후 다중 선택을 유지합니다.
- 새 미디어 자원을 먼저 준비해 실패/취소/오래된 상태에서 부분 분할·복제가 남지 않도록 했습니다. 복제 시 행 사이 간격과 최대 시간 경계도 확인합니다.
- Node 회귀 **120개 통과**, Python 서버 **7개 통과**. 네이티브 이벤트 모의 검사이며 실제 브라우저 UI·완성 MP4 검수는 미실행입니다. 운영 main은 병합하지 않습니다.

## 이전 변경 · 2026-08-28 브라우저 자동 편집

- 왼쪽 모자이크·무음 컷 메뉴와 선택 클립 속성의 자동 편집 바로가기를 추가했습니다. 유료 API 연결 상태로 버튼을 숨기거나 비활성화하지 않습니다.
- 원본 화면에서 최대 4개 사각형을 지정하고 강도·여유를 조절합니다. 로컬 Worker가 양방향 특징 추적을 수행하며, 원본 PTS와 프레임 지속시간을 저장합니다. 실패·미추적 구간은 회색 처리하고 내보내기를 차단합니다.
- 추적은 원본 시각 기준으로 저장하며 버전 4 프로젝트에 포함됩니다. 분할·트림·이동·undo/redo 및 미리보기/내보내기 공통 렌더러에 연결했습니다. 투명 원본과 블러 배경에서도 가리기 전 디테일을 남기지 않습니다.
- 원본 다채널 PCM에서 무음 후보를 검출하고 파형·후보 선택·남길 소리 미리듣기를 제공합니다. 적용 시 새 클립을 먼저 준비한 뒤 선택 트랙만 원자적으로 변경합니다. 취소·오래된 결과·전체 무음·1,000클립 초과는 원본을 유지합니다.
- Supertonic 2 한국어 10보이스를 WASM으로 실행해 WAV를 만들고 소재함/오디오 트랙에 추가합니다. 설치된 Web Speech 로컬 음성은 미리듣기 전용으로 구분합니다.
- Whisper Tiny 다국어 모델을 브라우저에서 실행합니다. 실제 단어 시각을 기준으로 자막을 만들고 검토/교정 후 새 영상 트랙에 추가합니다. 기존 자막은 유지합니다.
- AI 모델은 명시적 실행 후 고정 revision에서 다운로드합니다. 원고·PCM 업로드와 API 키가 없습니다. 모델/엔진의 최초 다운로드 크기·이용 조건·AI 음성 고지 의무를 표시합니다.
- TTS/ASR의 서로 다른 고정 ORT 버전을 vendor 폴더로 분리했습니다. npm 의존성이나 빌드 단계는 추가하지 않았습니다. Windows 로컬 서버가 .mjs/.wasm MIME을 명시합니다.
- Node 79개·Python 7개 통과. 실제 WASM TTS/ASR 2개 한국어 사례 실행. 브라우저 조작·완성 MP4·사람이 듣는 음질 검수는 미실행이며 운영 main과 stt-worker는 보존합니다.

## 이전 변경 · 2026-08-28 번호형 트랙·변형·소재 확장

- 영상·자막·그래픽을 영상 1·2·3…로, 음악·보이스·효과음을 오디오 1·2…로 통합했습니다. 종류별 24개까지 늘리고 호환 트랙 사이로 이동합니다.
- 기존 자동 연결은 저장 시각을 바꾸지 않고 해제합니다. 트림·이동·분할·잔물결은 같은 트랙 기준입니다.
- 빈 공간을 선택해 S/Delete로 닫습니다. 삭제된 공백 선택의 재사용은 거부합니다.
- 렌더러는 모든 영상 트랙을 아래부터 합성하고, 트랙별 디졸브와 여러 동시 자막을 지원합니다. 투명도는 완성 요소에 한 번 적용합니다.
- 모든 시각 요소의 위치·가로/세로 크기·회전·반전·투명도·4방향 크롭·중앙 정렬과 모니터 조절을 추가했습니다.
- 안전영역 4종과 수동 여백 조절, 전환 ＋ 및 프레임 이동 버튼의 CSS 아이콘 정렬을 추가했습니다.
- OFL 한국어 폰트 30종, 모션 그래픽 20종, 자막 16종, 직접 합성하는 효과음 20종. Myinstants는 외부 탐색 링크만 연결합니다.
- 문서는 버전 3으로 저장하며 버전 1·2를 불러옵니다. 글꼴 실패/취소를 내보내기에 전달합니다.
- Node 56개, Python 7개 통과. 실제 브라우저 조작·반응형 화면·완성 MP4 검수는 남았습니다. main 병합 없음.

## 이전 변경 · 2026-08-28 타임라인 편집

- 영상 클립별 절대 시작 시각을 저장하여 자유 이동과 빈 공간을 지원합니다. 구버전 문서를 읽고 버전 2로 저장합니다.
- S는 선택한 클립만 분할합니다. Delete는 공백 유지, Shift+Delete는 선택한 트랙만 당겨 삭제합니다.
- 자막·그래픽 직접 이동은 영상 연결을 해제하고 길이를 보존합니다. 전체 길이는 모든 트랙의 마지막 시각입니다.
- 두 영상 사이에 선택 가능한 전환 버튼을 표시하며, 클릭하면 실제 효과·길이가 반영된 전환 패널을 엽니다.
- 소재 드래그 중 대상 트랙·시작·끝·길이와 뒤 영상 이동량을 표시합니다. 표시와 확정은 같은 계산을 사용합니다.
- 새 timeline-edits.js가 배치·분할·삭제·트림·전환 명령을 모읍니다. 드래그 중 상태를 바꾸지 않고 놓을 때 한 번 적용합니다.
- 분할 시 이미지 모션과 영상·오디오 페이드를 유지하며, 저장/undo에서도 복원합니다.
- Node 36개와 Python 7개 검사 통과. 실제 브라우저 조작 및 완성 MP4 검수는 남아 있습니다.
- 이번에는 TTS/STT, 트래킹 모자이크, 무음 자동 컷을 변경하지 않았습니다.

## 새 진입점

`public/studio.html`, `public/studio.css`, `public/js/studio-app.js`가 새 UI입니다. 기존 `public/index.html`, `public/style.css`, `public/js/main.js`는 덮어쓰지 않았습니다. 기존 UI를 새 코어와 함께 정식 사용하도록 검수한 것은 아닙니다.

## 추가 모듈

공유 브랜치의 README에는 실험판 진입 안내를 붙였고 `.gitignore`에 Python 캐시·환경 파일·개인 `.shorts` 저장 파일을 제외했습니다.

- `project-store.js`: 미디어 자원과 편집 문서 분리, undo/redo, IndexedDB, 소재 포함 파일 저장, 원자적 복원.
- `timeline.js`: 트랙 렌더링, 사각 다중 선택, 묶음 이동, 클립 순서·트림, 스냅·확대·파형.
- `batch-edits.js`: 선택 집합·호환 설정 복사·일괄 편집·자원 준비 후 원자적 분할/복제.
- `monitor-editor.js`: 출력 캔버스 밖의 선택 테두리와 직접 변형·자르기 조작.
- `presets.js`: 그래픽 20종, 자막 16종, 전환 정의.
- `font-catalog.js`, `sound-effects.js`, `safe-areas.js`, `visual-transform.js`: 폰트·합성 효과음·참고 영역·공통 변형.
- `ai-client.js`: 다중 채널 WAV 변환, 실제 음성 타임스탬프 기반 자막.
- `studio-tools.js`, `silence.js`: 검토 후 적용하는 자동 편집 화면과 다채널 무음 분석.
- `mosaic.js`, `mosaic-worker.js`, `video-analysis.js`: 원본 프레임 추적·가림·경로 검증.
- `local-ai.js`, `tts-worker.js`, `asr-worker.js`, `model-download.js`: 취소 가능한 로컬 추론과 공개 모델 캐시.
- `studio_server.py`: 로컬 정적 서버와 명시적 MIME. 이전 선택형 OpenAI 프록시도 남아 있으나 새 편집기에서는 호출하지 않음.

## 복사본의 기존 코어 수정

- `state.js`: 전환 겹침 시간표·레이어·음량 가중치, 번호형 트랙 레지스트리와 독립 항목 시각.
- `render.js`: 여러 트랙과 트랙별 전환을 투명 오프스크린 캔버스에서 합성, 그래픽·자막 스타일. 미리보기/내보내기 공통.
- `player.js`: 전환 중 두 영상 동기화, 독립 오디오, 프레임마다 BGM 페이드 갱신, undo와 분리한 비동기 디코더 상태.
- `audio.js`: 전환 크로스페이드, 번호형 오디오 트랙 믹싱, 자막용 믹스에서 배경음악 제외.
- `exporter.js`: 출력 프레임마다 각 트랙의 활성 소스를 디코딩하여 합성. faststart와 무음 오디오 트랙 유지.

## 운영 반영 전

브라우저 조작 및 실제 MP4 검수, HEVC/MKV 등 샘플 조합 검사, 한국어 음질 비교, 제품 UI 피드백이 남았습니다. 이번 로컬 AI는 정적 브랜치 미리보기에서 실행하도록 구현했으며 Python 프록시 이식이나 비밀키가 필요 없습니다. 모델 최초 다운로드·브라우저 캐시·기기별 속도는 실제 브라우저에서 추가로 확인해야 합니다.

운영 반영은 승인 후 별도 브랜치와 PR로 진행해야 합니다. 원본 `stt-worker`, `wrangler.jsonc`, YouTube 업로드 설정은 변경하지 않았습니다.
