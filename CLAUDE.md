# 숏츠 스튜디오 — AI 에이전트용 안내

이 파일은 **AI 코딩 도구가 읽는 문서**다. 사람용 설명은 [README.md](README.md),
협업 규칙은 [CONTRIBUTING.md](CONTRIBUTING.md) 에 있다.

작업을 시작하기 전에 이 파일을 끝까지 읽어라. 아래 "이미 부딪혀 본 함정" 절에 적힌 것들은
전부 실측으로 확인한 것이라, 모르고 건드리면 같은 곳에서 다시 깨진다.

---

## 이 프로젝트가 무엇인가

세로 영상(9:16) 숏츠·릴스를 **브라우저 안에서만** 만드는 편집기다.
영상·이미지·음악이 서버로 전송되지 않고, 디코딩부터 인코딩까지 전부 사용자 PC에서 돈다.

**https://shorts-studio-75p.pages.dev**

## 절대 어기면 안 되는 제약

| 제약 | 이유 |
|---|---|
| **빌드 단계를 만들지 마라** | `npx serve public` 하나로 뜨는 게 이 프로젝트의 성질이다. 번들러·트랜스파일러를 도입하지 마라 |
| **npm 의존성을 추가하지 마라** | 외부 라이브러리는 `public/vendor/` 에 파일로 넣는다 (현재 mediabunny 하나뿐) |
| **파일을 서버로 보내지 마라** | 사용자 미디어가 로컬을 벗어나면 이 도구의 존재 이유가 없어진다. 예외는 자동 자막(오디오만, 사용자가 버튼을 눌렀을 때) |
| **API 키를 클라이언트에 넣지 마라** | 정적 사이트라 다 보인다. 키가 필요하면 Worker 를 거친다 |
| **주석은 한국어로 쓴다** | 기존 코드가 전부 한국어 주석이다. 섞지 마라 |

## 구조

```
public/
  index.html      전체 UI (탭 구조: 클립 / 텍스트 / 자막 / 오디오 / 쇼핑 / 템플릿 / 내보내기)
  style.css
  vendor/mediabunny.min.js   WebCodecs 래퍼. 건드리지 말 것 (MPL-2.0)
  js/
    main.js       1300줄. UI 배선 전부. 새 기능은 대개 여기에 붙는다
    state.js      project 객체 = 단일 진실. 타임라인 계산도 여기
    render.js     프레임 렌더러. 미리보기와 내보내기가 공유한다 (아래 참고)
    player.js     미리보기 재생 (requestAnimationFrame)
    exporter.js   내보내기 (WebCodecs, 실패 시 MediaRecorder 폴백)
    audio.js      오디오 믹싱 + 자막 빈 구간 탐지
    media.js      파일 -> 클립 (2단계 폴백 있음)
    shopping.js   쇼핑 쇼츠 장면 구성 (⚠️ 아래 "미완성" 참고)
    youtube.js    유튜브 업로드 (OAuth 팝업 + 재개 가능 업로드)
    transcribe.js 자동 자막 클라이언트 (Worker 호출)
    srt.js, util.js
stt-worker/       자동 자막 서버 (Cloudflare Worker). Pages 와 별개로 수동 배포
```

### 핵심 설계: 렌더러 하나를 공유한다

`render.js` 의 `renderFrame(ctx, t, opts)` 를 **미리보기와 내보내기가 똑같이** 쓴다.
다른 건 프레임을 어디서 가져오냐뿐이다.

| | 프레임 소스 | 구동 |
|---|---|---|
| 미리보기 | `<video>` 엘리먼트 | requestAnimationFrame (실시간) |
| 내보내기 | mediabunny `VideoSampleSink` | 프레임 루프 (실시간보다 빠름) |

**이 구조를 깨지 마라.** 렌더링 로직을 양쪽에 나눠 쓰면 "화면과 결과물이 다르다" 는
버그가 반드시 생긴다. 새 시각 요소는 `renderFrame` 안에 넣어야 내보내기에도 나온다.

### 상태 모델

`state.js` 의 `project` 하나가 전부다.

```js
project = {
  width, height, fps, quality,     // 출력 설정
  clips: [],        // 순서대로 이어붙는 영상/이미지
  overlays: [],     // 시간 구간을 가진 텍스트 (start, end)
  captions: [],     // 자막. overlays 와 구조는 비슷하나 스타일이 공통
  captionStyle: {}, // 자막 전체 공통 스타일
  template: {},     // 밴드 레이아웃 (mode: 'none' | 'band')
  audio: {},        // 원본 볼륨 + 배경음악
}
```

시간 계산은 전부 `state.js` 의 함수를 쓴다 (`clipAt`, `totalDuration`, `clipStartTime`).
직접 계산하지 마라 — 트림·이미지 길이 처리가 섞여 있다.

---

## 이미 부딪혀 본 함정

**전부 실측으로 확인한 것이다.** 되돌리면 같은 곳에서 다시 깨진다.

### 내보내기

- **소리가 없어도 무음 AAC 트랙을 넣는다.** 오디오 트랙이 아예 없는 mp4 는
  인스타 릴스가 처리에 실패한다. `exporter.js` 의 `makeSilence()`
- **moov 를 파일 앞으로 보낸다** (`fastStart: 'in-memory'`). 뒤에 있으면
  릴스 컨테이너가 ERROR 로 떨어진다
- **프레임 루프의 양보는 `setTimeout` 이 아니라 MessageChannel 로 한다.**
  `setTimeout(0)` 은 백그라운드 탭에서 1초까지 늘어나 인코딩이 6배 느려진다.
  `exporter.js` 의 `yieldToUi()`

### 파일 불러오기

- **영상은 2단계로 연다.** `<video>` 로 먼저 시도하고, 실패하면 mediabunny
  디코더로 다시 시도한다 (`media.js` 의 `viaVideoElement` -> `viaDecoder`).
  MPEG-TS 처럼 브라우저가 모르는 컨테이너, moov 가 끝에 있는 초대형 파일이 여기서 살아난다
- 디코더 모드 클립은 `clip.decoderOnly = true` 이고 `<video>` 가 없다.
  미리보기 소리가 안 나오지만 내보내기에는 정상적으로 들어간다

### 자동 자막

- **말이 흐려지면 Whisper 는 틀리게 받아쓰는 게 아니라 그 구간을 통째로 버린다.**
  VAD 를 꺼도, 문턱값을 낮춰도 안 살아난다 (둘 다 시도해 봤다).
  그래서 `audio.js` 의 `findUncaptioned()` 가 "소리는 나는데 자막이 없는" 구간을 찾아 표시한다
- **RMS 문턱값을 피크 기준으로 잡으면 안 된다.** 웅얼거린 구간이 0.008 인데
  또렷한 말이 0.07 이라, "피크의 몇 %" 로 자르면 정작 찾아야 할 구간이 묵음 처리된다.
  무음 바닥 기준 절대값(-48dBFS)을 쓴다
- **음성 인식에 보낼 때는 배경음악을 뺀다** (`mixTimeline({ includeBgm: false })`).
  노래가 섞이면 정확도가 눈에 띄게 떨어진다

### 타임라인 드래그

- **끄는 동안 `renderTimeline()` 을 부르면 안 된다.** 잡고 있던 엘리먼트가 사라져서
  드래그가 끊긴다. 위치만 직접 바꾸고, 목록·패널은 손을 뗄 때 맞춘다
- **드래그 뒤에 따라오는 click 을 무시해야 한다.** 안 그러면 드래그를 끝낼 때마다
  재생 위치가 튄다 (`node._dragged` 플래그)
- 최소 길이 블록은 손잡이 둘이 블록을 다 덮으므로 `min-width: 30px` 을 준다

### 미리보기

- **탭이 가려지면 재생을 멈춘다.** `requestAnimationFrame` 이 멈춰서, 돌아왔을 때
  시간이 훌쩍 뛴다 (`visibilitychange` 핸들러)
- 반복 재생은 기본 꺼짐이다. 편집 중에는 한 번 보고 멈추는 편이 낫다

### 워커 (stt-worker)

- CORS 허용 목록에 `localhost` 와 `127.0.0.1` **둘 다** 있어야 한다.
  같은 서버라도 주소를 다르게 치는 경우가 있다
- Workers AI 의 MeloTTS 는 **동작하지 않는다** (영어도 3043 에러). TTS 를 붙일 거면
  외부 서비스 + Worker 프록시로 가야 한다

---

## 미완성 / 알려진 문제

### `shopping.js` 는 실제 포맷과 맞지 않는다

이미지 여러 장을 슬라이드쇼로 만드는데, **실제 쇼핑 쇼츠는 제품이 작동하는 영상**을 쓴다.
정지 이미지로는 시청 유지가 안 된다. 다시 짜야 하는 상태다.

방향은 "영상 1개 + 구간별 자막 + 훅 문구" 이고, 밑에 깔린 장면 계획·오버레이 배치
로직(`planScenes`, `buildOverlays`)은 그대로 쓸 수 있다.

### 유튜브 업로드는 비공개로만 올라간다

감사(compliance audit)를 통과하지 않은 API 프로젝트로 올린 영상은 유튜브가
비공개로 잠근다. 코드 문제가 아니라 정책이다. 고치려 하지 마라.

### 인스타 발행은 이 저장소에 없다

메타 API 는 로컬 파일을 안 받고 공개 URL 만 받는다. 브라우저에서 직접 호출도 CORS 로 막힌다.
발행은 별도 로컬 프로젝트(`instagram-autopost`)의 CLI 로 하고, **토큰이 들어 있어
공유하지 않는다.** 이 저장소에 인스타 관련 코드를 추가하지 마라.

---

## 작업할 때

### 확인 방법

**브라우저에서 실제로 돌려서 확인하라.** 이 프로젝트는 캔버스 렌더링과 미디어 인코딩이
핵심이라, 코드만 읽고는 맞는지 알 수 없다.

```bash
npx serve public
```

시각적 변경은 **픽셀을 세서** 확인하는 게 확실하다. 예를 들어 자막이 제대로 들어갔는지는
내보낸 영상에서 해당 시각의 프레임을 뽑아 자막 영역의 흰 픽셀 수를 세면 된다.
"보기에 맞는 것 같다" 로 넘어가지 마라.

### 커밋

무엇을 바꿨는지보다 **왜 바꿨는지**를 쓴다. 코드를 보면 무엇은 알 수 있지만 왜는 알 수 없다.
실측한 숫자가 있으면 같이 남긴다.

### 배포

`main` 에 합쳐지면 Cloudflare 가 자동 배포한다. **`wrangler pages deploy` 를 직접 쓰지 마라.**
Git 연결과 직접 업로드를 섞으면 어느 쪽이 현재 배포본인지 추적이 안 된다.

`stt-worker/` 만 예외로 수동 배포이고, 배포하면 **모든 사용자의 자동 자막에 즉시 영향**을 준다.
혼자 실험하지 말고 상의하고 올려라.
