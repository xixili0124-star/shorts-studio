# Shorts Studio · 로컬 실험판

형제 공동 검토용 1차 작업본입니다. 공유용 `codex/studio-lab` 브랜치에 올리는 별도 시제품이며 운영 `main`에는 병합하지 않습니다. 운영 서비스를 바꾸는 직접 배포도 하지 않습니다.

## 형에게 공유한 브랜치

GitHub의 `codex/studio-lab` 브랜치에서 **Code → Download ZIP**으로 내려받거나 다음처럼 복제하면 됩니다.

```powershell
git clone --branch codex/studio-lab --single-branch https://github.com/xixili0124-star/shorts-studio.git shorts-studio-lab
cd shorts-studio-lab
```

실험판은 `9eb4ef1`을 기준으로 만들었습니다. 이후 운영 `main`에 추가된 `ee8e325`의 TTS 작업은 덮어쓰거나 섞지 않았습니다. 두 버전의 통합은 UI 검토 후 별도 작업으로 진행합니다.

## 실행

Windows에서 이 폴더의 `start-studio.cmd`를 실행하고 **http://127.0.0.1:8787/studio.html** 을 Chrome 또는 Edge로 여세요. 실행 창을 닫으면 서버가 종료됩니다. 폴더를 옮겼다면 해당 폴더에서 다시 실행하면 됩니다.

Python 3.10 이상이 필요합니다. 이 PC에서는 Codex에 포함된 Python을 사용하며, 다른 PC에서는 Python을 설치하면 됩니다. 별도 npm 설치나 빌드는 없습니다.

직접 실행할 때는 이 폴더에서 아래 명령을 사용합니다.

```powershell
python studio_server.py --port 8787
```

`public/studio.html`을 파일로 직접 열지 마세요. 미디어 처리·프로젝트 저장·AI 프록시는 로컬 HTTP 서버를 전제로 합니다. 서버는 이 PC에서만 접근 가능한 127.0.0.1에 연결합니다. 이 주소는 형의 PC와 공유할 수 있는 공개 링크가 아닙니다.

## 먼저 둘러보기

1. 11초짜리 샘플 프로젝트에서 Space로 재생해 보세요. 샘플은 사진 3장과 합성 테스트 사운드입니다.
2. **소재**에서 영상·이미지·오디오를 함께 가져오세요. 파일은 소재함에 먼저 보관되고, 더블클릭하거나 타임라인으로 끌면 편집에 추가됩니다.
3. 타임라인에서 클립 가운데를 끌어 순서를, 양 끝을 끌어 구간을 바꾸세요. **S**는 재생 위치에서 분할, **Ctrl+Z**는 실행 취소입니다.
4. 왼쪽 **그래픽 / 자막 / 전환**에서 효과를 선택하고 오른쪽 **속성**에서 문구·색상·시간·위치를 조절하세요.
5. **저장**은 소재를 포함한 `.shorts` 프로젝트, **내보내기**는 완성 영상 파일을 만듭니다. 자동으로 외부 플랫폼에 게시하지 않습니다.

## 이번 작업 범위

| 영역 | 실험판에 구현한 내용 |
| --- | --- |
| UI | 소재함 / 프로그램 모니터 / 속성 / 하단 타임라인의 편집기 구조 |
| 소재 | 영상·이미지·오디오 통합 가져오기, 필터, 이름 검색, 동일 소재 반복 사용 |
| 타임라인 | 그래픽 G1, 자막 C1, 영상 V1, 오디오 A1, 보이스 A2, 파형, 확대, 스냅, 분할·복제·삭제 |
| 그래픽 | 팝 타이틀, 로어 서드, 스와이프, 포커스, 카운트다운, 인용구 6종 |
| 자막 | 8개 프리셋, 개별/전체 적용, 텍스트·폰트·색상·위치·등장 효과, SRT 입출력 |
| 전환 | 디졸브, 블랙 페이드, 화이트 플래시, 컷; 미리보기와 내보내기의 동일 렌더러 사용 |
| 저장 | 실행 취소/다시 실행, 브라우저 자동 저장, 소재 포함 `.shorts`, 실패 시 이전 프로젝트 유지 |
| AI | OpenAI TTS·자동 자막 프록시와 UI. 실제 호출·음질 검증은 미실시 |

전환은 인접 클립을 실제로 겹치므로 전체 영상이 그만큼 짧아집니다. 짧은 클립의 전환은 양쪽 클립 길이 절반 이내로 제한합니다. 클립에 연결된 자막·그래픽은 순서 변경과 트림을 따라가며, SRT 등 연결되지 않은 항목은 시퀀스 시각을 유지합니다.

## AI 음성 및 자동 자막

현재 API 키는 설정되어 있지 않습니다. **목소리 품질을 확인한 완제품 상태가 아니며, 연결 준비 단계**입니다.

서버 실행 환경의 `OPENAI_API_KEY`를 설정하고 재시작하면 기능이 활성화됩니다. 키를 HTML·JavaScript·Git·프로젝트 파일·채팅에 넣지 마세요. 설정 유무만 화면에 표시하며 서버 연결 성공 여부와는 구분합니다.

- 음성 생성은 `gpt-4o-mini-tts`, 13개 보이스, 말하기 지시, 0.75–1.25배 속도, WAV 결과를 사용합니다.
- 자동 자막은 `whisper-1`의 실제 단어·구간 타임스탬프를 사용합니다. 영상 음성과 보이스 트랙을 보내며 배경음악은 제외합니다.
- 버튼 실행 전 전송 내용과 별도 API 비용에 대한 확인을 받습니다. 취소해도 서버에서 이미 처리한 요청은 과금될 수 있습니다.
- 생성 음성은 소재함과 A2에 추가됩니다. 게시할 때 AI 생성 음성임을 알려야 합니다.
- 실제 한국어 샘플을 듣고 발음·억양·호흡을 비교한 뒤 보이스를 선정하는 과정이 남아 있습니다.

공식 참고: [Text to speech](https://developers.openai.com/api/docs/guides/text-to-speech), [Speech to text](https://developers.openai.com/api/docs/guides/speech-to-text).

## 저장과 제한

자동 저장은 **이 브라우저·이 로컬 주소**의 IndexedDB에 보관됩니다. 다른 브라우저, 포트, `localhost` 주소로 바꾸면 별도 저장 공간입니다. 브라우저 데이터를 지우면 자동 저장도 없어집니다. 중요한 작업은 `.shorts` 파일로 별도 저장하세요.

파일당 600MB, 소재 200개, 자동 저장 총 소재 300MB까지 제한합니다. 브라우저 저장 용량이 부족하면 수동 파일 저장을 안내합니다. 영상 길이 이후의 오디오는 출력에 포함하지 않습니다.

이번 타임라인은 **영상 1개 트랙** 중심입니다. 여러 영상 레이어 합성, 속도 램프, 키프레임 편집, 색 보정, 효과음 라이브러리, 클라우드 공동 편집은 아직 구현하지 않았습니다. 같은 오디오 행에서 클립이 겹치면 A1/A2를 나누어 사용하세요.

내보내기는 브라우저 WebCodecs 지원이 필요합니다. 지원 코덱에 따라 MP4 또는 WebM을 사용합니다. 디코더 폴백 영상은 미리보기의 원본 소리가 지원되지 않을 수 있습니다. HEVC/MKV 등은 기기·브라우저 코덱에 따라 다릅니다.

## 검증 상태와 다음 단계

계산·캔버스 합성·프로젝트 오류 복구·서버 보호 테스트 23개와 JS 문법 검사가 통과했습니다. **브라우저에서 실제 파일을 가져와 조작하고, MP4를 완성해 재생하는 전체 검수는 아직 하지 않았습니다.** TTS·STT도 가짜 응답으로 서버 동작만 검사했고 실제 유료 호출은 하지 않았습니다.

자세한 범위는 `VERIFICATION.md`, 코드 변경 범위는 `CHANGELOG-LAB.md`를 참고하세요. 화면 방향이 마음에 들면 실제 브라우저 조작·출력·한국어 음질 검수 후 필요한 변경만 별도 브랜치/PR로 옮기는 순서를 권합니다. **이 폴더 전체를 운영에 바로 배포하지 마세요.**
