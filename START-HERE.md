# Shorts Studio · 사용 안내

`codex/studio-lab`과 [PR #1](https://github.com/xixili0124-star/shorts-studio/pull/1)에서 시작한 새 멀티트랙 편집기입니다. 기존 첫 화면도 유지합니다. 이 문서는 `main`의 기능을 설명하며, 공유 주소는 푸시와 자동 배포가 끝난 뒤 갱신됩니다.

## 형에게 공유할 주소

**[새 편집기 열기](https://shorts-studio-75p.pages.dev/studio.html)** · [기존 편집기](https://shorts-studio-75p.pages.dev/) · [브랜치 미리보기](https://codex-studio-lab.shorts-studio-75p.pages.dev/studio.html)

정식 주소는 `main`의 Git 연결을 통한 Cloudflare Pages 자동 배포 완료 후 갱신됩니다. 개인 소재는 브라우저 안에 보관하며 자동으로 Git에 올리지 않습니다. 두 사람이 동시에 같은 프로젝트를 편집하는 기능은 없으므로, 소재를 포함한 .shorts 프로젝트 파일로 주고받으세요. PC AI를 쓰려면 공개 주소가 아니라 설치한 PC 실행기의 로컬 주소를 사용합니다.

## 2026-08-28 · 편집 기능 확장

### 클립 이동과 맞바꾸기

| 조작 | 결과 |
| --- | --- |
| 같은 행의 기존 클립 하나를 빈 공간으로 이동 | 클립 전체 길이가 들어가면 그대로 놓습니다. 다른 클립은 밀지 않습니다. |
| 같은 행의 기존 클립 하나를 다른 클립 위로 이동 | 두 클립의 순서만 맞바꾸고 각 길이와 나머지 클립의 시각을 유지합니다. |
| 맞바꾸거나 이동한 결과가 다른 클립과 충돌 | 배치하지 않고 이유를 알립니다. 공간을 확보하거나 전환 겹침을 해제한 뒤 다시 시도하세요. |
| 여러 클립을 묶어서 이동 | 각자의 행과 시간 간격을 유지합니다. 선택하지 않은 클립과 겹치면 거부하며, 맞바꾸기는 하지 않습니다. |
| 새 소재 추가 또는 기존 클립을 다른 행으로 이동 | 빈 곳에는 그대로 배치하고, 점유된 곳에는 가까운 경계로 삽입합니다. 필요한 경우 대상 행의 뒤 항목만 밀며 초록색 범위에서 실제 위치·길이·이동량을 먼저 확인합니다. |

영상 이동이 다른 행의 자막·그래픽·음악을 함께 끌고 가지 않습니다. 삭제는 **Delete**로 공백 유지, **Shift+Delete**로 해당 행만 당기기, 선택한 공백은 **S / Delete**로 닫기를 사용하세요.

전환이 있는 영상끼리 교환하면 연결점의 전환도 새 순서에 맞춰 이어집니다. 바뀐 클립 길이로 기존 겹침을 유지할 수 없는 경우에는 다른 클립을 밀지 않고 교환을 거절합니다.

### 키프레임으로 움직임·음량 조절

1. 클립을 선택하고 재생 막대를 원하는 시각에 놓습니다. 오른쪽 **키프레임 → 속성 → ◇ 키 추가**에서 키를 만듭니다.
2. 다른 시각으로 이동해 키를 추가하고 값을 바꿉니다. 위치·가로/세로 크기·회전·불투명도를 조절할 수 있고, 영상 소리와 오디오는 **음량 0~300%**를 지원합니다.
3. 키가 있는 속성은 오른쪽 설정이나 모니터 드래그로 현재 시각의 값을 편집합니다. 키프레임 줄의 ◆를 끌거나 시각 입력칸으로 키를 옮기고, ×로 삭제합니다. 양옆 화살표로 이전·다음 키로 이동합니다.
4. **부드럽게 연결**은 두 키 사이를 선형으로 바꾸고, **다음 키까지 유지**는 다음 키가 올 때까지 값을 유지합니다.

키 시각은 클립 시작을 0초로 저장합니다. 클립을 옮겨도 움직임은 함께 이동하고, 트림·분할은 잘린 경계의 값을 보존합니다. 높은 음량에서는 소리가 찌그러질 수 있으므로 재생하며 확인하세요.

### 대상을 따라가는 크롭 트래킹

**영상 선택 → 속성 → 자동 편집 → 크롭 트래킹 · 대상 따라가기**를 엽니다. 원본 화면에서 사람·동물 등 따라갈 대상을 사각형으로 직접 지정하고, 시각을 옮겨 기준 프레임과 확대 1~3배를 조절한 뒤 추적을 실행하세요. 작은 9:16 화면에서 결과를 확인하고 **적용**해야 프로젝트에 반영됩니다. 분석을 취소하거나 창을 닫으면 적용하지 않으며, 같은 창에서 기존 추적도 제거할 수 있습니다. 원본 파일은 바꾸지 않습니다.

별도 AI 모델이 대상을 자동으로 찾아내는 기능은 아닙니다. 지정한 영역의 무늬와 윤곽을 추적하며 **한 번에 최대 3분**입니다. 가림·급격한 이동·낮은 대비·장면 전환에서는 대상을 놓칠 수 있고, 실패한 구간은 마지막 위치를 유지하며 경고합니다. 구간을 나누어 다시 지정하거나 위치 키프레임으로 보정하세요. 미리보기는 선택 영상만 보여주므로 전체 합성은 프로그램 모니터에서 확인합니다.

### 자막과 트랙 표시

자동자막을 검수한 뒤 **자동자막 트랙에 추가**하면 전용 행을 처음 만들고 이후 같은 행을 재사용합니다. 기본 빈 프로젝트에 불필요한 자동자막 행을 만들지 않으며, 기존 수동 자막은 지우지 않습니다. 빈 자동자막 행은 마지막 한 줄도 제거할 수 있습니다.

첫 보이스 트랙의 배지는 **V**입니다. 새 단축키를 뜻하지 않습니다. 자막 속성에서 중복되는 표시 구간·배치 트랙·자르기 설정은 숨겼습니다. 시각·길이·행 이동은 타임라인에서 조절하고, 자막·그래픽의 모니터 편집은 **변형**을 사용합니다. 영상·이미지에는 기존 **변형 / 자르기** 스위치를 유지합니다. 자막 등장과 퇴장은 각각 **13옵션(12효과 + 없음)**에서 독립적으로 선택합니다.

## 2026-08-28 · PC Turbo 자동자막

PC 자막 엔진으로 **Whisper large-v3-turbo**를 추가했습니다. 모바일·공유 웹주소의 Whisper Tiny, 기존 편집과 TTS는 유지합니다. 별도 ASR 환경 설치, 실제 GPU 한국어 인식, 취소·재실행, 브라우저에서 새 자막 트랙 추가·실행 취소를 확인했습니다. 다른 PC에서는 아래 설치 과정이 필요합니다.

1. 중요한 작업을 .shorts 파일로 저장하고, Windows 64비트 PC의 최신 실험판 폴더에서 **`setup-pc-asr.cmd`**를 실행합니다. 시작용 Python 3.10 이상이 필요하며 자막 전용 Python 3.11 환경을 준비합니다. Vox·GPT-SoVITS 환경에 패키지를 덧씌우지 않습니다.
2. 공개 모델 약 1.62GB와 별도 실행 환경 다운로드에 동의합니다. GPU 설치에는 CUDA 라이브러리 다운로드 약 570MB가 추가되며 설치·캐시를 위해 디스크 여유 공간 8GB 이상을 확보하세요. 모델·환경은 Git과 배포 ZIP에 넣지 않습니다.
3. 기본 장치는 NVIDIA **`cuda` / `int8_float16`**입니다. GPU 없는 PC는 **`setup-pc-asr.cmd --device cpu`**로 `cpu` / `int8`를 명시 선택합니다. GPU 실패를 CPU나 외부 API로 자동 대체하지 않으며 CPU 처리 속도는 검증하지 않았습니다.
4. 기본 편집기는 **`start-studio.cmd`**, 내 목소리 TTS도 함께 쓸 때는 **`start-pc-voice.cmd`**를 실행하고 표시된 로컬 주소를 엽니다. 기존 실행기가 켜져 있으면 프로젝트를 저장하고 종료한 뒤 최신 실행기로 다시 시작하세요.
5. **자막 → 인식 엔진 → PC 고정밀 · Whisper large-v3-turbo → PC 연결 확인**에서 설치·연결 상태와 CPU/GPU·계산 형식을 확인합니다. 처음 연결에서 설치가 확인되면 Turbo가 기본 선택되지만 사용자가 고른 Tiny 선택은 유지합니다.
6. 인식할 클립 또는 전체 말소리를 선택하고 **자동 자막 만들기**를 실행합니다. 오디오를 같은 PC 서버로 보내는 확인창에 동의한 경우에만 인식합니다. 문구·시각·원문을 검수한 뒤 **자동자막 트랙에 추가**합니다. 전용 행은 재사용하며 기존 자막은 지우지 않습니다.

### PC 자막의 범위·개인정보

- **한 번에 최대 3분**입니다. 선택 클립은 잘라낸 원본 소리를 인식하며 편집기의 음소거와 무관합니다. 전체 말소리는 영상 소리와 용도가 `voice`인 오디오를 섞고 배경음악·효과음·음소거한 클립은 제외합니다. 일반 오디오로 등록한 말소리는 해당 클립을 선택해서 실행하세요. 트랙 행의 이름만 바꾼다고 인식 용도가 바뀌지는 않습니다.
- 브라우저는 16kHz 모노 PCM16 WAV를 **현재 PC의 동일 출처 localhost 서버에만** 전송합니다. 공개 페이지나 모바일에서 임의의 PC 엔진을 탐색하지 않습니다. API 키·외부 음성 서비스는 사용하지 않습니다.
- 인식용 오디오는 별도 파일로 저장하지 않고 서버·작업 프로세스 메모리로 전달합니다. 결과는 검수용으로 서버 메모리에 임시 보관하며, 적용한 자막만 일반 편집 문서에 넣습니다. 프로젝트의 원본 미디어 보관 방식은 기존과 같습니다.
- 단어 시각을 얻지 못했어도 **실제 문장 시각**이 있으면 문장 전체를 한 자막으로 살리고 경고합니다. 단어 단위 시각을 가정해 나누지 않습니다. 전체 인식 원문과 자막을 함께 확인하고 숫자·고유명사·소음 구간을 교정하세요.

### TTS와 GPU 메모리

같은 PC 실행기에 연결된 TTS와 ASR 요청은 순서대로 처리합니다. 자막은 작업마다 별도 프로세스를 사용하고 작업이 끝나면 프로세스를 종료해 ASR GPU 메모리를 반환하도록 구현했습니다. 그 때문에 매번 로컬 모델을 불러오는 준비 시간이 포함됩니다.

GPU 여유가 충분하면 Vox 모델을 유지합니다. 부족할 때만 Vox를 GPU에서 내리고, 다음 TTS 요청에서 로컬 모델을 다시 불러옵니다. 자막 종료나 상태 조회만으로 Vox를 다시 올리지 않으며 모델 재다운로드·참고 녹음 변경도 하지 않습니다. 다른 게임·편집기·AI 프로세스를 강제로 종료하지 않습니다.

**작업 취소**는 결과 적용을 막고 PC 자막 작업 종료를 요청합니다. 종료 여부가 불확실하다는 메시지가 나오면 프로젝트를 저장하고 PC 실행기를 다시 시작하세요. 이 동작은 아래의 내 목소리 TTS 취소와 구분됩니다. [앱 안의 PC Turbo 안내](public/pc-asr-setup.html)에서 설치·실행 순서를 다시 볼 수 있습니다.

## 이전 업데이트 · PC 확장 내 목소리 TTS · VoxCPM2

기본 편집·브라우저 TTS·자동자막을 유지하면서 **VoxCPM2 2B**를 PC 내 목소리 기본 엔진으로 추가했습니다. 기존 GPT-SoVITS v2ProPlus와 참고 녹음은 보존합니다. 공개 주소나 모바일에서는 기존 기능을 쓰고, PC에서 내 목소리를 생성해 .shorts 파일로 이동하면 모바일에서도 그 음성을 편집할 수 있습니다. 다른 기기의 실시간 공동 편집·자동 동기화는 아닙니다.

1. 작업 중이라면 .shorts 파일로 저장합니다.
2. Windows PC에서 실험판을 별도 폴더에 완전히 풀고 `setup-pc-voice.cmd`를 실행합니다. 시작용 Python 3.10 이상이 필요하며, 별도 Python 3.11과 Vox 모델 약 5GB 및 수 GB의 런타임을 다운로드합니다. 기본은 NVIDIA CUDA용이며 CPU는 `--device cpu`로 선택합니다. 15GB 이상 여유 공간을 확보하세요. 이미 GPT를 설치했어도 Vox용 환경은 따로 준비합니다.
3. `start-pc-voice.cmd` 실행 후 표시되는 로컬 주소를 엽니다. 기본 포트 8787이 이미 사용 중이면 기존 작업을 저장한 뒤 기존 서버를 종료하거나 `start-pc-voice.cmd --port 8788`로 따로 실행합니다. 실행기는 다른 서버를 종료하지 않습니다.
4. **AI 음성 → 실행 방식 → PC 확장 · 내 목소리 → PC 연결 확인 → 내 목소리 등록**에서 마이크 또는 파일로 3~10초 참고 음성을 준비합니다. 마이크 녹음은 최대 9초이며, 녹음에서 실제로 읽은 문장을 함께 입력합니다.
5. 등록한 목소리 선택 → 새 원고와 속도 → 사용 동의 → 생성 → 미리듣기 → 보이스 트랙에 추가합니다. 기존 등록을 다시 녹음할 필요는 없습니다. 별도 TTS 사이트로 이동하지 않습니다.

Vox는 48kHz WAV를 생성하며 속도는 생성 후 음높이를 유지해 조절합니다. 점수 `4대 2`는 `사 대 이`로 처리하지만 일반 개수 `차량 4대`는 그대로 둡니다. 짧은 문장부터 비교하고 날짜·외래어·이름은 들어보고 확인하세요. 참고 녹음은 잡음 없는 5~10초와 정확한 문장을 권장합니다.

이전 엔진 비교: 작업 저장 → 실행기 종료 → `start-pc-voice.cmd --provider gpt-sovits`. Vox로 돌아갈 때는 `--provider voxcpm2`를 사용합니다. 처음 GPT를 설치할 때만 `setup-gpt-voice.cmd`가 필요합니다. GPU 메모리를 위해 보통 한 엔진만 실행하세요. 설정에 provider가 없는 기존 설치는 GPT로 인식하며, Vox 설치가 완료되기 전 기본 설정을 덮어쓰지 않습니다.

[앱 안의 PC 사용 안내](public/pc-voice-setup.html)에서 자세한 순서와 문제 해결을 확인할 수 있습니다. 자동 설치는 Windows 64비트 범위이며 Mac·모바일에서 Python 엔진 설치를 자동화하지 않습니다. NVIDIA GPU가 없으면 CPU로 생성할 수 있으나 해당 성능은 검증하지 않았습니다.

**참고 기반 복제이며 개인 모델 학습은 아닙니다.** 본인·허락받은 음성만 사용하세요. 목소리의 닮은 정도와 한국어 억양은 녹음·문장에 따라 달라지므로 생성 후 들어봐야 합니다. 이 PC의 RTX 3060에서 합성 참고 음성으로 Vox 48kHz WAV 생성 5개 사례를 확인했으며, 사용자 목소리·사람이 듣는 품질은 아직 검증하지 않았습니다. 검증용 녹음만 삭제했고 기존 개인 녹음은 바이트까지 보존했습니다.

### 참고 녹음의 보관과 취소

- 참고 녹음과 읽은 문장은 `.studio-local/voices`에 별도 보관하고 소재함·자동 저장·.shorts·Git·배포 ZIP에 넣지 않습니다. 등록창 위 파일 드롭·붙여넣기도 소재 가져오기와 분리합니다.
- 완성된 음성만 일반 오디오로 프로젝트에 넣습니다. .shorts를 모바일에 옮겨 이어서 편집할 수 있으며, 참고 목소리 목록은 PC 사이에 자동 동기화하지 않습니다.
- 삭제는 저장된 참고 WAV·문장을 지웁니다. 생성한 WAV·프로젝트 사본은 유지되며 엔진의 메모리는 엔진을 종료할 때 해제됩니다. Windows 파일 잠금으로 삭제가 일부 실패하면 목록에 남겨 재시도할 수 있습니다.
- 저장·삭제 창을 닫아도 시작한 디스크 작업은 완료될 수 있으며 목록을 다시 확인합니다. 생성 취소는 결과 수신 취소이며 엔진 추론을 즉시 중단하지 않습니다. 응답이 불확실한 경우 엔진·편집기 서버 재시작을 요구합니다.
- 공개 웹주소는 localhost를 탐색하지 않습니다. PC 어댑터는 동일 출처·사용 동의를 확인하고, 엔진은 루프백과 개인 키로 제한하며 원본 관리 API는 막습니다. 사용자 원고·참조 경로가 콘솔에 남지 않도록 실행기에서 출력을 폐기합니다. 설치와 추론은 분리하고 추론 프로세스의 외부 Python 네트워크 접근을 막습니다.

## 모자이크 추적을 놓친 구간

클립 위쪽에 **붉은 빗금**이 보이면 그 구간은 추적을 놓친 곳입니다.
빗금을 누르면 그 지점으로 재생 막대가 이동하니, 모자이크가 대상을 제대로 덮고 있는지 확인하고
필요하면 그 지점에서 다시 추적하세요.

놓친 구간도 처음 지정한 사각형으로 가리기는 하지만 대상이 움직였다면 빗나갈 수 있습니다.
화면에서는 주황 점선 테두리로 표시됩니다. 이 구간이 남아 있으면 내보내기가 막힙니다.

## 미리보기가 작을 때

세로 영상은 **화면 높이가 곧 미리보기 크기**입니다. 노트북처럼 화면이 낮으면 다음 두 가지를 쓰세요.

- **경계 끌기** — 타임라인 맨 위 가장자리를 위아래로 끕니다. 고른 높이는 이 브라우저가 기억합니다.
  마우스 없이는 그 경계를 탭으로 선택한 뒤 ↑↓(Shift 를 누르면 크게), Home·End 로 조절합니다.
- **⛶ 크게 보기** — 미리보기 머리말의 버튼입니다. 타임라인을 최소로 접어 한 번에 크게 봅니다.
  다시 누르면 직전 높이로 돌아갑니다.

## 자동 자막 엔진

**자막 → 인식 엔진** 에서 고릅니다. 기본은 **서버 · Whisper large-v3-turbo** 입니다.

| 엔진 | 준비 | 정확도 | 소리 |
| --- | --- | --- | --- |
| 서버 · large-v3-turbo | 없음 | 높음 | 자막 서버로 보냄 |
| 브라우저 · Tiny | 첫 실행 66MB | 낮음 | 기기 밖으로 안 나감 |
| PC · large-v3-turbo | 1.62GB 설치 | 높음 | 같은 PC 안에서만 |

서버 엔진은 **선택한 구간의 소리만** 16kHz 모노 WAV 로 보냅니다. 영상 파일은 보내지 않고
서버는 결과만 돌려줍니다. 소리를 내보내고 싶지 않으면 브라우저 엔진을 고르세요 —
대신 작은 모델이라 한국어를 자주 틀립니다.

## 소재 지우기

소재 카드에 마우스를 올리면 오른쪽 위에 **×** 가 나옵니다. 그 소재를 라이브러리에서 지웁니다.
여러 개를 한 번에 정리하려면 라이브러리 위의 **안 쓰는 소재 N개 비우기** 를 누르세요.

**타임라인에서 쓰고 있는 소재는 지워지지 않습니다.** 그 소재의 × 는 눌리지 않으며,
지우려면 타임라인에서 해당 클립을 먼저 지워야 합니다.

소재를 지우면 **되돌리기 기록이 초기화됩니다.** 되돌리기가 지워진 소재를 다시 불러올 수 없어서
어중간하게 깨진 상태로 두지 않기 위해서입니다.

## 트랙 스위치

트랙 머리의 작은 버튼입니다.

- `◉` **숨김**(영상 계열) — 화면에서만 뺍니다. 클립에 붙은 소리는 그대로 납니다.
- `M` **뮤트**(오디오) — 미리보기와 내보내기 양쪽에서 뺍니다.
- `S` **솔로**(오디오) — 그 트랙만 듣습니다. **미리보기에서만** 적용되고 내보내기는 따르지 않습니다.
  솔로를 켜 둔 채 내보내도 배경음악이 빠지지 않습니다. 정말 빼려면 뮤트를 쓰세요.
- `L` **잠금** — 그 트랙의 클립을 선택할 수 없게 만듭니다. 화면과 소리는 그대로입니다.
  실수로 끌어 옮기고 싶지 않은 트랙에 걸어 두세요.

## 긴 영상·큰 파일을 넣을 때

소재 파일은 하나당 **2GB** 까지 넣을 수 있습니다.

타임라인에 영상을 올리면 원음을 별도 오디오 클립으로 분리하는데, 이때 소리를 통째로 펼쳐야 해서
**48kHz 스테레오 기준 약 5분 50초**(5.1 채널은 약 2분)가 한계입니다. 이보다 긴 영상은
**분리만 건너뛰고 영상 클립에 소리를 담은 채로** 추가합니다. 소리는 그대로 나오고 내보내기·자동자막에도
포함되지만, 소리만 따로 옮기거나 볼륨을 따로 조절할 수는 없습니다.
원음을 따로 다루고 싶다면 필요한 구간만 잘라서 가져오세요.

분리에는 길이에 따라 수십 초가 걸립니다. 진행률이 안내에 표시되며, 그동안 편집기는 멈춘 것이 아닙니다.

## 영상과 소리 연결 · 우클릭 메뉴

영상을 타임라인에 올리면 원음이 별도 오디오 클립으로 분리됩니다. 이 둘은 **연결된 상태**로 추가되어
프리미어처럼 함께 움직입니다. 연결된 클립에는 블록 왼쪽 아래에 ⛓ 표시가 붙습니다.

| 조작 | 동작 |
| --- | --- |
| 연결된 클립 하나를 클릭 | 묶음 전체가 함께 선택되고 표시됩니다 |
| 연결된 클립을 드래그 | 묶음 전체가 시간 간격을 유지한 채 함께 이동합니다. 잡은 클립만 다른 행으로 옮길 수 있고 나머지는 원래 행에 남습니다 |
| 연결된 클립의 양 끝을 드래그 | 묶음 전체를 **같은 이동량**만큼 함께 자릅니다. 한쪽이 먼저 한계에 닿으면 그 값에 모두를 맞춥니다 |
| 연결된 클립에서 S / Ctrl+D / Delete | 분할·복제·삭제도 묶음 전체에 적용합니다. 잘라낸 오른쪽 조각끼리, 복제한 사본끼리 다시 연결됩니다 |
| 클립 우클릭 | 클립 메뉴를 엽니다. 메뉴 키·Shift+F10 으로도 열리며 ↑↓·Esc 로 조작합니다 |
| 우클릭 메뉴 → 연결 해제 (Ctrl+L) | 묶음을 풀어 화면과 소리를 따로 편집합니다 |
| 두 개 이상 선택 → 연결 (Ctrl+L) | 직접 고른 영상·오디오 클립을 하나로 묶습니다 |

**연결은 구조 편집에만 적용합니다.** 이동·트림·분할·복제·삭제는 묶음 전체에 걸리지만,
색·크기·음량·페이드 같은 속성은 클릭한 클립에만 적용합니다. 영상 페이드를 조절했다고
원음까지 페이드되면 곤란하기 때문입니다.

연결 정보는 프로젝트 파일(.shorts)에 함께 저장하며, 짝을 지우고 혼자 남은 연결은 연결 없음으로 취급합니다.
직접 연결한 두 클립의 끝이 원래 어긋나 있었다면 함께 자를 때도 그 간격을 그대로 지킵니다.

## 이전 업데이트 · 다중 선택과 직접 편집

| 조작 | 동작 |
| --- | --- |
| 빈 공간에서 누른 채 드래그 | 사각형에 걸친 여러 클립을 선택. Shift 드래그는 기존 선택에 추가, Ctrl 드래그는 선택 반전 |
| Shift / Ctrl 클릭 | 개별 클립의 선택 추가·해제. Ctrl+A는 입력칸 밖에서 타임라인 전체 선택 |
| 여러 클립 선택 후 속성 변경 | 호환되는 선택 클립에 변경한 값만 적용. 서로 다른 값은 “여러 값” 표시 |
| 여러 클립 선택 후 Delete / Shift+Delete | 모두 삭제. Delete는 공백 유지, Shift+Delete는 해당 행들의 뒤 클립만 당김 |
| 여러 클립 선택 후 S / Ctrl+D | 재생 막대가 지나가는 선택 클립만 분할 / 트랙 사이 시간 간격을 유지해 복제 |
| 선택 묶음 가운데 드래그 | 각자 트랙과 시간 간격을 유지해 함께 이동. 선택하지 않은 클립과 겹치면 배치하지 않음 |
| 각 클립 위 설정 복사 / 붙여넣기 | Ctrl+Alt+C / Ctrl+Alt+V. 선택된 묶음 안에서 붙여넣으면 묶음 전체에 호환 설정 적용 |
| 영상·그래픽·자막 행의 ＋ | 해당 행 바로 위에 같은 용도의 트랙 추가 |
| 오디오·보이스 행의 ＋ | 해당 행 바로 아래에 같은 용도의 트랙 추가 |
| 모니터 위 변형 / 자르기 스위치 | 영상·이미지의 화면 조작 모드 변경. 자막·그래픽은 변형만 제공 |
| 변형 모드 | 내부 드래그로 이동, 모서리로 비율 유지 확대, 변으로 한 방향 확대, ↻로 회전 |
| 자르기 모드 | 영상·이미지의 경계 드래그로 숨길 양 조절, 내부 드래그로 잘라낸 창의 위치 이동 |
| 가로 중앙 / 세로 중앙 | 잘라낸 뒤 보이는 요소의 중심을 정렬. 이동 중 중앙에 가까우면 안내선에 스냅 |
| Esc / Ctrl+Z | 진행 중 드래그 취소 / 확정한 작업 한 번 되돌리기 |

모니터에서 Shift를 누르면 모서리 확대의 비율 제한을 풀고 회전은 15° 단위가 됩니다. Alt는 이동 중 중앙 스냅을 해제합니다. 오른쪽의 숫자·슬라이더 설정도 그대로 사용할 수 있습니다. 여러 시각 클립을 선택했다면 화면 조작을 함께 적용합니다. 오디오는 화면 변형에서 제외합니다.

**설정 복사는 소재·문구·시각·길이·트랙·전환 연결·모자이크·크롭 추적을 바꾸지 않습니다.** 화면 변형·영상 크롭, 클립 맞춤·페이드, 글자 스타일, 음량·음소거와 호환되는 키프레임을 옮깁니다. 키는 붙여넣을 클립의 길이에 맞추며, 해당 속성의 기존 키를 교체합니다. 추적 영역은 원본마다 다르므로 복사하지 않습니다. 아주 짧은 클립은 마우스를 올리거나 키보드로 포커스하면 상단 설정 버튼이 펼쳐집니다.

트랙 이름은 **영상·그래픽·자막·오디오·보이스**이며, 추가한 행은 같은 이름에 2·3…을 붙입니다. 이름은 기본 추가 위치를 정할 뿐, 호환 계열 사이의 클립 이동을 제한하지 않습니다. 기본 용도의 마지막 행은 유지하고, 그 외 빈 행은 ×로 제거합니다. 선택형 **자동자막** 행은 비어 있으면 마지막 한 줄도 제거할 수 있습니다. 상단 종류 선택 + 트랙 버튼으로 구형 프로젝트에 없는 용도를 다시 만들 수 있습니다. 영상 계열과 오디오 계열은 각각 최대 24행입니다.

기존 버전 4의 트랙 ID·순서·클립 시각은 유지하고 용도 정보만 보충합니다. 프로젝트 형식은 그대로 버전 4입니다.

## 자동 편집 도구

소재가 없어도 왼쪽 **모자이크·무음 컷·AI 음성·자막** 메뉴가 보입니다. 영상/오디오 클립을 선택하면 오른쪽 속성의 **자동 편집**에서도 열 수 있습니다.

| 기능 | 사용 순서 | 처리 방식 |
| --- | --- | --- |
| 크롭 트래킹 | 영상 선택 → 속성의 자동 편집 → 크롭 트래킹 · 대상 따라가기 → 원본 대상 지정 → 추적 → 미리보기 → 적용 | 지정한 영역을 따라 세로 화면 이동. 최대 3분, 확대 1~3배. 실패·가림 구간은 확인·보정 필요 |
| 트래킹 모자이크 | 영상 선택 → 모자이크 → 원본 화면에 사각형 드래그 → 현재 위치에서 자동 추적 → 결과 확인 → 적용 | 원본 좌표·원본 시각으로 저장. 강도 1~100, 가림 여유 조절, 최대 4개 영역 |
| 무음 구간 자동 컷 | 영상/오디오 선택 → 무음 컷 → 음량·최소 무음·말 앞뒤 여유 조절 → 후보 체크 → 남길 소리 확인 → 적용 | 선택한 클립의 원본 소리를 분석하고 같은 트랙의 뒤 항목만 당김 |
| 브라우저 TTS | AI 음성 → 브라우저 AI · 기본 음성 → 목소리·속도·품질·원고 → 이용 조건 동의 → 음성 만들기 → 들어보기 → 보이스 트랙에 추가 | Supertonic 2, 한국어 10보이스, 실제 44.1kHz WAV 생성. 파일 다운로드도 가능 |
| 자동자막 | 자막 → 인식 엔진 → 선택 클립 또는 전체 말소리 → 만들기 → 문구·시각 확인 → 자동자막 트랙에 추가 | 브라우저 Tiny 또는 설치된 PC Turbo 사용. 전용 행 재사용. Turbo는 단어 시각이 없으면 실제 문장 시각으로 보존. 기존 자막을 지우지 않음 |

### 모자이크 확인하기

- 자동 얼굴 인식이 아니라, 직접 지정한 사각형의 무늬와 윤곽을 추적합니다. 급격한 이동·다른 물체와의 겹침·장면 전환·낮은 대비에서는 위치를 다시 지정해야 합니다.
- 실패하거나 추적하지 않은 구간은 해당 원본 레이어 전체를 회색으로 가립니다. 이 상태의 클립은 내보내기를 막습니다. 결과 미리보기를 끄고 원본에서 대상을 다시 지정해 추적하거나, 고정 영역으로 바꿀 수 있습니다.
- 이미지는 고정 모자이크를 지원합니다. 가림 강도가 커질수록 블록이 커지며 영역은 불투명합니다. 블러 배경·크롭·회전에도 가려진 소스를 사용합니다.
- 원본 시각으로 저장하므로 분할·트림·이동 후에도 같은 원본 위치를 가립니다. 추적한 구간 바깥으로 트림을 늘리면 추가 추적이 필요합니다.
- 추적은 약 10Hz 표본 사이를 보간합니다. 추적기가 놓친 잘못된 위치까지 자동으로 판별하지는 못하므로, 민감한 얼굴·번호판은 게시 전 모든 구간을 확인하세요.
- **.shorts에는 가리기 전 원본도 들어 있습니다.** 가려진 결과를 공유하려면 완성 MP4를 사용하세요.

### 무음 컷·자막의 범위

한 번에 최대 3분을 처리합니다. 긴 파일은 필요한 구간으로 트림하거나 분할해 주세요. 무음 컷은 볼륨·음소거·페이드와 무관하게 원본 모든 채널을 확인하며, 소리를 읽지 못한 구간을 무음으로 지우지 않습니다. 겹친 장면 전환은 먼저 제거해야 합니다. 전체가 무음이거나 남는 구간이 한 프레임보다 짧으면 적용을 막습니다. 적용은 한 번의 실행 취소로 되돌릴 수 있습니다.

**다른 트랙은 따라오지 않습니다.** 무음 컷을 한 뒤 이미 있던 자막·음악의 싱크를 확인하거나 컷 편집 후 자동자막을 만드세요. Tiny·Turbo 모두 자동자막의 전체 범위는 영상 소리와 용도가 보이스인 오디오만 포함하고, 배경음악·효과음·음소거한 클립은 제외합니다. 일반 오디오로 등록한 말소리는 해당 클립을 선택하세요. 전체 범위는 편집한 음량·페이드도 반영하며, 영상에 이미 들어 있는 음악은 분리하지 않습니다. 선택 클립 범위는 음소거 여부와 무관하게 원본을 인식합니다. 오디오의 알려진 시작/끝 바깥은 빈 소리로 유지하지만, 실제 디코딩 누락은 오류로 알립니다.

### 기본 브라우저 TTS·자막을 처음 사용할 때

- 별도 서버 설정·로그인·API 키·API 이용료가 필요 없습니다. **최초 TTS 약 276MB, 자동자막 약 66MB**의 공개 모델·엔진 다운로드가 필요합니다. 통신 요금은 사용자 인터넷 환경에 따릅니다.
- 원고·영상·음성은 외부로 보내지 않습니다. 브라우저의 별도 작업 프로세스에서 WASM으로 계산하며 GPU가 필수는 아닙니다. 모델 파일은 Hugging Face에서, 실행 엔진은 이 실험판 주소에서 받습니다.
- 모델은 브라우저 캐시에 보관합니다. 저장 공간 부족·시크릿 모드·캐시 삭제 시 다음 실행에서 다시 다운로드할 수 있습니다. 모델 준비 중에도 취소할 수 있습니다.
- TTS의 **설치된 기기 음성** 모드는 다운로드 없이 로컬 음성으로 읽는 미리듣기 전용입니다. Web Speech API는 WAV를 돌려주지 않으므로, 영상에 넣을 파일은 **기기 내 AI** 모드에서 생성하세요. 로컬 음성이 설치되지 않은 기기에서는 이 모드를 사용할 수 없습니다.
- 한국어 문장 생성과 인식은 실제 WASM으로 확인했지만, 숫자·날짜·금액·이름은 틀릴 수 있습니다. 먼저 듣고 자막을 교정하세요. 휴대폰·저사양 기기의 처리 속도는 검증하지 않았습니다.
- Supertonic 2 모델은 MIT가 아닌 **OpenRAIL-M** 조건을 따릅니다. 이용 제한을 지키고, 결과를 게시할 때 AI 생성 음성임을 표시해야 합니다. 원문 조건은 앱 동의란과 [소재·엔진 출처](CREDITS.md)에 연결되어 있습니다.

## 확장 트랙과 편집 도구

| 조작 | 동작 |
| --- | --- |
| 영상·그래픽·자막 | 영상·이미지·자막·그래픽을 같은 종류의 트랙에 배치. 위쪽 영상 트랙이 화면에서도 위에 표시 |
| 오디오·보이스 | 음악·보이스·효과음을 함께 배치. 오디오 용도는 속성에서 별도로 지정 |
| 행별 ＋ / 상단 ＋ 트랙 | 같은 용도의 행 추가. 기본 용도의 마지막 행은 유지하며, 빈 자동자막 행은 모두 제거 가능 |
| 트랙 이름 클릭 | 소재 더블클릭·＋·프리셋 클릭 시 추가할 대상 트랙 선택 |
| 같은 행의 기존 클립 하나 드래그 | 빈 곳에는 다른 클립을 밀지 않고 이동, 다른 클립 위에서는 둘만 맞바꾸기. 충돌하는 배치는 거부 |
| 호환되는 다른 행으로 클립 이동 | 빈 곳은 그대로 배치, 점유된 곳은 가까운 경계에 삽입하고 대상 행 뒤 항목만 이동 |
| 클립 선택 → S | 재생 막대가 있는 선택 클립들을 분할 |
| 빈 공간 선택 → S / Delete | 선택한 공백을 닫고 해당 트랙의 오른쪽 클립만 당김 |
| 클립 선택 → Delete | 선택한 클립들을 삭제하고 빈 공간 유지 |
| Shift+Delete / 당겨 삭제 | 선택한 트랙의 뒤 항목만 당겨 삭제. 영상·자막·그래픽이 섞여 있어도 같은 트랙 기준 |
| 클립 양 끝 드래그 | 반대쪽 끝을 고정하고 구간 조절. 원본 및 같은 트랙 이웃 범위 유지 |
| 두 미디어 사이 ＋ / 전환 아이콘 | 선택 테두리와 장면 전환 패널. Delete로 전환 제거 |
| 소재·효과음 드래그 | 놓기 전에 실제 위치·길이·대상 트랙·밀리는 항목 수 표시 |
| 변형 / 화면 자르기 | 모든 시각 요소의 위치·가로/세로 크기·회전·반전·불투명도 조절. 4방향 크롭은 영상·이미지에 제공 |
| 키프레임 | 위치·크기·회전·불투명도·음량의 시간별 값 추가·이동·삭제, 선형 연결 또는 다음 키까지 유지 |
| 가로 중앙 / 세로 중앙 | 잘라낸 뒤 보이는 요소의 중심을 화면 중앙에 정렬 |
| 모니터의 선택 요소 드래그 | 화면 위치 이동, 모서리 드래그로 비율을 유지하며 확대 |
| 격자 아이콘 | Shorts / Reels / TikTok / 공통 안전영역 선택과 개별 여백 조절 |
| Ctrl+Z / Ctrl+Shift+Z | 트랙 추가·이동·분할·삭제·전환·변형 실행 취소 / 다시 실행 |

구형 프로젝트의 영상 연결 정보는 해제하고, 저장되어 있던 자막·그래픽 시각과 길이는 유지합니다. 영상 이동·트림이 다른 트랙을 끌고 가지 않습니다.

영상이 없는 구간은 검은 배경이며, 그 시각에 있는 자막·그래픽·오디오는 유지됩니다. 프로젝트 끝은 모든 트랙의 마지막 항목 기준입니다. 전환은 같은 트랙에서 맞닿은 두 영상/이미지의 실제 겹침에만 적용하며, 길이는 각 클립의 절반 이내입니다.

## 폰트·템플릿·효과음

- OFL 폰트 **64종(한국어 37종·영문 27종)**. 실제 글꼴을 불러와 목록에서 모양을 미리 봅니다. 영문용 글꼴에 없는 한글은 기기의 대체 글꼴로 표시됩니다.
- 모션 그래픽 **32종**, 자막 스타일 **16종**, 전환 **15옵션(14효과 + 바로 연결)**. 자막 등장·퇴장은 각각 **13옵션(12효과 + 없음)**에서 따로 고릅니다.
- Kenney **CC0 효과음 샘플 24종**: 스우시·딸깍·알림 등 짧은 영상용 소리를 WAV로 제공합니다. API 없이 미리듣기·추가·드래그할 수 있고, 이전 프로젝트의 저장된 음원도 유지합니다.
- Myinstants는 사용자가 제공한 [app 사운드 페이지](https://www.myinstants.com/ko/instant/app/)로 외부 탐색만 연결합니다. 음원 수집·자동 다운로드·프록시·임베드·상업 이용권 제공은 하지 않습니다. 이용 권한이 확인된 파일만 소재함에 가져오세요.

글꼴 다운로드가 실패하면 미리보기에 대체 글꼴이 보일 수 있습니다. 내보내기는 필요한 글꼴이 준비되지 않으면 오류를 알리고 중단합니다. 인터넷 연결 후 다시 시도하거나 다른 폰트를 선택하세요. 자막 내용은 폰트 URL에 보내지 않습니다. 폰트·효과음의 원본과 사용 조건은 [CREDITS.md](CREDITS.md)에 연결되어 있습니다.

안전영역은 광고 가이드에서 참고한 권장 사각형이며, 모든 기기·앱 UI·긴 설명문을 보장하지 않습니다. 특히 Reels는 2023 Meta 템플릿을 참고했습니다. 가이드는 미리보기에만 보이고 내보낸 영상에 포함되지 않습니다. 출처와 사용 조건은 [CREDITS.md](CREDITS.md)에 있습니다.

## 로컬 실행

Windows에서는 start-studio.cmd를 실행한 뒤 http://127.0.0.1:8787/studio.html 을 Chrome 또는 Edge로 여세요. 이 주소는 다른 PC에서 접속할 수 없습니다. Python 3.10 이상이 필요하며 별도 npm 설치나 빌드는 없습니다.

```powershell
git clone --branch main --single-branch https://github.com/xixili0124-star/shorts-studio.git shorts-studio
cd shorts-studio
python studio_server.py --port 8787
```

public/studio.html을 파일로 직접 열지 마세요. 서버 창을 닫으면 로컬 서비스가 종료됩니다.

## 작업 보관과 호환성

새로고침 전에 중요한 작업은 **저장** 버튼으로 .shorts 파일에 보관하세요. 자동 저장은 현재 브라우저·주소·포트의 IndexedDB에 남습니다. 공개 미리보기와 로컬은 각각 별도 저장 공간입니다.

이전 버전 1·2·3 프로젝트를 열 수 있습니다. 새로 저장하는 **버전 4**에는 트랙·용도·변형·크롭·키프레임·모자이크·크롭 추적 경로가 포함되므로 **구버전 편집기에서는 열 수 없습니다.** 같은 버전 4라도 이전 편집기는 새 필드를 보존하지 않을 수 있으니 원본 프로젝트를 따로 보관하세요.

파일당 600MB, 소재 200개, 자동 저장 총 소재 300MB로 제한합니다. 원본 미디어는 변경하지 않습니다.

## 남은 범위와 운영 반영

전문 편집기의 덮어쓰기·트랙 잠금·동기화 잠금은 아직 지원하지 않습니다. 잔물결 삭제는 선택한 한 트랙만 당깁니다.

실험판의 자동 편집은 원본 Cloudflare 자동자막 서버와 분리되어 있습니다. 브라우저 Tiny는 서버에 음성을 보내지 않고, PC Turbo만 사용자 실행 동의 후 같은 PC의 `/api/pc-asr` 경로를 사용합니다. 기존 Python 서버에 남아 있는 선택형 외부 API 프록시로 자동 전환하지 않습니다. API 키를 채팅·Git·브라우저 코드에 넣지 마세요.

기능별 회귀 검사와 실제 추론·브라우저 검수 범위는 [VERIFICATION.md](VERIFICATION.md)에서 구분합니다. **Turbo는 합성 한국어 두 사례와 자막 검수·새 트랙 추가·undo/redo를 확인했습니다.** 이는 실제 녹음 전체의 인식률 보장이 아니며, 모바일 실기·이번 변경 후 MP4 완성 파일 검수는 별도입니다.

실험판 출발점은 9eb4ef1이며, main 통합 이력은 [CHANGELOG-LAB.md](CHANGELOG-LAB.md)에 남깁니다. 이번 변경은 원격 배포 확인 전입니다. 형제 검토와 실제 출력 확인 후 `main` 푸시·Pages 자동 배포를 통해 공유 주소에 반영합니다. 원본 `stt-worker`의 별도 배포는 포함하지 않습니다.
