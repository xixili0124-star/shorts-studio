# Shorts Studio · 공동 검토용 실험판

운영 main은 그대로 두고 codex/studio-lab 브랜치에서 작업합니다. 검토용 [Draft PR #1](https://github.com/xixili0124-star/shorts-studio/pull/1)은 병합하지 않습니다.

## 형에게 공유할 주소

**[실험판 열기](https://codex-studio-lab.shorts-studio-75p.pages.dev/studio.html)**

Git 연결을 통한 Cloudflare의 실험 브랜치 미리보기입니다. 운영 사이트와 주소가 다릅니다. 타임라인 편집은 브라우저 안에서 처리하며, 두 사람이 같은 프로젝트를 실시간으로 함께 편집하는 기능은 없습니다. 프로젝트를 주고받으려면 소재를 포함한 .shorts 파일로 저장하세요.

## 2026-08-28 타임라인 수정

| 조작 | 동작 |
| --- | --- |
| 클립 선택 → S | 재생 막대가 선택한 클립 안에 있을 때 그 항목만 분할. 다른 트랙은 유지 |
| Delete | 선택한 항목만 삭제하고 빈 공간 유지 |
| Shift+Delete / 당겨 삭제 | 선택한 트랙에서만 뒤 클립을 삭제 길이만큼 당김. 오디오는 선택한 A1 또는 A2만 적용 |
| 영상 클립 가운데 드래그 | 빈 곳에 자유롭게 배치. 다른 영상 위에 놓으면 가까운 앞/뒤 경계에 삽입하고 뒤 영상을 이동 |
| 자막·그래픽 드래그 | 길이를 유지하며 영상 범위 밖까지 이동. 기존 연결 옵션은 직접 이동 시 해제 |
| 클립 양 끝 드래그 | 반대쪽 끝을 고정하고 구간 조절. 이웃 영상이나 원본 길이를 넘지 않음 |
| 두 영상 사이 ＋ / 전환 아이콘 | 테두리로 선택 표시, 장면 전환 패널 열기. Delete로 전환 제거 |
| 소재함에서 드래그 | 놓기 전에 초록색 범위에 실제 위치·길이·대상 트랙 표시. 이미지 3초, 영상·오디오는 원본 길이 |
| 소재 더블클릭 / ＋ | 재생 막대 위치를 기준으로 추가, 새 클립 강조 |
| Ctrl+Z / Ctrl+Shift+Z | 위치·분할·삭제·전환을 실행 취소 / 다시 실행 |

영상이 없는 구간은 검은 화면이며, 해당 시각에 있는 자막·그래픽·오디오는 계속 유지됩니다. 프로젝트 끝은 모든 트랙의 마지막 항목 기준입니다.

전환은 두 영상을 실제로 겹치는 방식입니다. 겹침의 중앙에 선택 버튼이 있고, 전환 길이를 바꾸면 뒤쪽 영상 트랙이 차이만큼 이동합니다. 빈 공간을 건너뛰는 전환은 지원하지 않습니다. 전환 길이는 양쪽 영상 길이의 절반 이내로 제한됩니다.

## 로컬 실행

Windows에서는 start-studio.cmd를 실행한 뒤 http://127.0.0.1:8787/studio.html 을 Chrome 또는 Edge로 여세요. 이 로컬 주소는 다른 PC에서 접속할 수 없습니다.

Python 3.10 이상이 필요하고 별도 npm 설치나 빌드는 없습니다. 직접 실행하려면:

```powershell
git clone --branch codex/studio-lab --single-branch https://github.com/xixili0124-star/shorts-studio.git shorts-studio-lab
cd shorts-studio-lab
python studio_server.py --port 8787
```

public/studio.html을 파일로 직접 열지 마세요. 서버 창을 닫으면 로컬 서비스가 종료됩니다.

## 작업 보관과 호환성

새로고침하기 전에 중요한 작업을 **저장** 버튼으로 .shorts 파일에 보관하세요. 자동 저장은 현재 브라우저·주소의 IndexedDB에 남습니다. 공개 미리보기와 로컬 주소, 서로 다른 포트는 각각 별도 저장 공간입니다.

이전 실험판의 버전 1 프로젝트를 열 수 있습니다. 새로 저장하는 버전 2에는 클립별 위치와 분할 시 모션·페이드 정보가 포함되므로 **구버전 편집기로 다시 열 수는 없습니다.** 원본 파일은 따로 보관하세요.

파일당 600MB, 소재 200개, 자동 저장 총 소재 300MB까지 제한합니다. 원본 미디어 자체는 수정하지 않습니다. 공개 미리보기 주소가 있어도 사용자가 가져온 개인 미디어가 Git이나 서버에 올라가는 것은 아닙니다.

## 남은 범위

현재는 영상 V1 한 줄과 그래픽 G1, 자막 C1, 오디오 A1, 보이스 A2 구조입니다. 다중 영상 트랙, 전문 편집기의 덮어쓰기·트랙 잠금·동기화 잠금 등은 아직 지원하지 않습니다. 당겨 삭제도 여러 트랙 전체를 자동으로 당기지 않습니다.

트래킹 모자이크, 무음 자동 컷, 무료 TTS 교체 및 기존 자동 자막 서버 재연결은 이번 타임라인 수정에 포함하지 않았습니다. 현재 AI 패널은 이전 실험판의 API 연결 준비 상태입니다. 유료 호출이나 개인 미디어 외부 전송을 실행하지 않았습니다.

공개 정적 미리보기에는 Python AI 프록시가 없으므로 현재 AI 패널만으로 TTS·자동 자막을 실행할 수 없습니다. 로컬 프록시도 별도 설정이 필요합니다. API 키를 채팅·Git·브라우저 코드에 넣지 마세요.

## 검증과 운영 반영

Node 회귀 검사 36개와 Python 서버 검사 7개가 통과했습니다. 선택 분할, 빈 구간, 전환, 드래그 위치 계산, 저장·복원, 공통 캔버스 렌더링을 검사했습니다.

**실제 브라우저의 마우스 조작, 화면 크기별 배치와 MP4 완성 파일 검수는 아직 남았습니다.** 정확한 범위는 [VERIFICATION.md](VERIFICATION.md)에 있습니다.

실험판의 출발점은 9eb4ef1입니다. main의 후속 TTS 작업 ee8e325를 자동 병합하거나 덮어쓰지 않았습니다. 형제 검토와 실제 출력 확인 후 충돌을 조정한 별도 PR로 운영에 반영해야 합니다.
