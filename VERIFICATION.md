# 검증 기록 · 2026-08-28

## 실행 결과

- Node 회귀 테스트 **36개 통과, 실패 0, 건너뜀 0**.
- Python 서버 테스트 **7개 통과**. 실제 AI 통신은 mock으로 대체.
- 실험판 JavaScript 모듈 11개 및 테스트 문법 검사 통과.
- Python 서버·테스트 컴파일 검사 통과.
- 로컬 서버 테스트에서 /studio.html, /js/timeline-edits.js, /api/ai/status HTTP 200.
- AI 테스트는 키 비노출, 설정 없음, 외부 Origin/Host 차단, 전송 동의, 입력 제한과 모의 응답을 확인. 유료 AI 호출 없음.

## 이번 타임라인 회귀 검사

- 선택한 영상·이미지·자막·그래픽·오디오만 분할. 다른 트랙 유지.
- 24/30/60fps에서 한 프레임 분할 후 잘못된 겹침 없음.
- 영상 빈 구간, 트랙별 독립 위치, 영상 뒤의 자막·그래픽·오디오 길이 반영.
- 기본 삭제와 선택 트랙/오디오 행만 당기는 삭제 구분.
- 디졸브 중간 삽입과 연결 해제, 원본 클립 길이 유지.
- 인접한 두 클립의 전환 변경·0초 제거, 뒤 영상 위치 변화.
- 드래그 메타데이터에서 이미지 3초·영상 원본 길이를 계산하고, 표시 계획과 실제 배치 일치.
- 원본 범위·이웃 클립을 지키는 트림과 반대쪽 끝 고정.
- 기존 연결 항목의 직접 이동 및 연결 대상 삭제 시 길이 보존.
- 분할된 이미지 모션·영상/오디오 페이드 유지와 오프라인 오디오 예약.
- 버전 1 문서의 버전 2 복원, 절대 위치·전환·모션·페이드 undo/redo.
- 실제 PNG 파일이 포함된 분할·빈 구간 프로젝트의 저장·재열기 왕복.
- 손상 파일·잘못된 겹침·비정상 시간 정보 거부, 실패 시 기존 프로젝트 복구.
- 800회 결정적 혼합 편집 후 시간표 불변식 확인.

캔버스 검사는 번들 @napi-rs/canvas에서 공통 renderFrame을 호출했습니다. 디졸브 중간 픽셀, 검정 페이드, 화이트 플래시, 그래픽 6종·자막 8종, 영상이 없는 구간의 검정 배경과 영상 뒤 자막을 확인했습니다. **브라우저 스크린샷이나 인코딩된 MP4 검사와는 다릅니다.**

## 아직 검사하지 않은 것

- 실제 브라우저의 클릭·드래그·키보드 전체 사용 흐름, 화면 크기별 레이아웃.
- 브라우저 IndexedDB 새로고침 복구와 실제 영상·오디오 코덱을 포함한 파일 왕복.
- 실제 MP4/WebM 내보내기 및 완성 파일의 화면·소리·자막 싱크.
- 긴 영상·큰 파일, 휴대폰 성능, HEVC/MKV 등 다양한 코덱 조합.
- TTS/STT 실제 인증·생성·전사 및 한국어 음질.

현재 사용자의 편집을 변경하는 브라우저 검수는 실행하지 않았습니다. 이 브랜치는 공동 검토용이며 운영 main에 병합하지 않습니다.

## 다시 실행

```powershell
python -m unittest discover -s tests -p 'test_*.py' -v
node --test tests/core.test.mjs
```

Node 검사 중 실제 캔버스가 필요한 4개는 @napi-rs/canvas가 없으면 건너뜁니다. 이미 설치된 모듈의 절대 경로를 STUDIO_CANVAS_MODULE 환경 변수로 전달할 수 있습니다. 편집기 실행에는 이 테스트 패키지가 필요하지 않습니다.
