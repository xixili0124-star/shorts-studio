# 검증 기록 · 2026-08-27

## 실제 실행하여 통과

- Node 테스트 **16개 통과, 실패 0, 건너뜀 0**.
- Python 서버 테스트 **7개 통과**. 실제 OpenAI 통신은 mock으로 대체.
- 변경/추가 JavaScript 모듈 10개 `node --check` 통과.
- Python 서버 컴파일 검사 통과.
- 로컬 `/studio.html`, `/js/studio-app.js`, `/api/ai/status` HTTP 200 확인.
- AI 상태: `configured=false`, `verified=false`, 보이스 목록 13개.
- 원본 참조 clone `git status --short` 결과 비어 있음, HEAD가 기준 SHA와 일치.

## 회귀 검사 내용

Node 검사는 4초+3초 클립의 1초 겹침=6초, 선형 가중치, 3중 겹침 방지, 클립 연결 자막의 이동·트림, 페이드, undo/redo의 미디어 자원 유지, 소재 포함 파일 포맷, 손상된 파일 거부 및 기존 작업 복구, 실제 PNG를 넣은 저장·재열기 왕복, 입력 식별자 검증, 오른쪽 채널 음성 처리, 실제 STT 시간 사용, 합성 샘플 WAV, 디코더 진행 중 undo, 독립 오디오 동기화를 검사합니다.

캔버스 검사는 번들 `@napi-rs/canvas`를 사용하여 공통 `renderFrame`을 직접 호출했습니다. 빨강/파랑 디졸브 중간 프레임이 약 (128, 0, 128, 255), 블랙 페이드는 검정, 화이트 플래시는 흰색인지 확인했습니다. 그래픽 6종과 자막 8종 모두 실제 픽셀을 그리는지도 검사했습니다. **이 검사는 브라우저 화면이나 인코딩된 MP4 검사와는 다릅니다.**

서버 검사는 다른 Origin/Host 차단, 전송 동의 헤더, 키 미설정, 입력 크기/형식, 보이스·속도 제한, 가짜 TTS 응답, 상태 API의 키 비노출, STT multipart 구성을 검사합니다.

## 아직 검사하지 않은 것

- 브라우저에서 실제 클릭·드래그·파일 가져오기·전체 사용 흐름 및 화면 크기별 레이아웃.
- 브라우저 IndexedDB를 통한 새로고침 후 복구 및 실제 소재 디코딩을 포함한 `.shorts` 왕복.
- 실제 영상 두 개가 겹친 MP4/WebM 내보내기, 완성 파일의 영상·소리·자막 싱크.
- 긴 영상/큰 파일의 메모리, 휴대폰 성능, 여러 코덱 조합.
- OpenAI 실제 인증·생성·전사 및 한국어 음성 품질. 외부 유료 호출 0회.

따라서 현재 상태는 **별도 검토용 1차 시제품**입니다. 위 항목을 정식 업데이트 전 확인해야 합니다.

## 테스트 다시 실행

Python 테스트는 추가 패키지가 없습니다.

```powershell
python -m unittest discover -s tests -p 'test_*.py' -v
node --test tests/core.test.mjs
```

Node 테스트 중 이미지·캔버스 3개는 `@napi-rs/canvas`가 없는 환경에서는 건너뜁니다. 이미 설치된 모듈의 절대 경로를 `STUDIO_CANVAS_MODULE` 환경 변수로 전달할 수도 있습니다. 앱 실행에는 이 테스트 패키지가 필요하지 않습니다.
