# 검증 기록 · 2026-08-28

## 2026-08-29 · 모델 기반 추적·PC 연결·영상 원음 분리

아래는 이번 변경에서 확인한 자동 검사와 실제 실행 결과입니다. 이전 검증 기록은 그대로 유지하며, 당시 사용한 추적 방식·모델과 이번 결과를 구분합니다. 설치 프로그램·공개 사이트의 PC 연결·배포 완료 검증은 아래 후속 확인란에 별도로 남깁니다.

### 자동 회귀 검사

- JavaScript 전체 **293개 통과, 실패 0, 건너뜀 0**. `STUDIO_CANVAS_MODULE`에 번들 `@napi-rs/canvas` 경로를 지정하고 `node --test tests/*.test.mjs`를 기본 파일별 격리로 실행했습니다.
- 브라우저 추적 모델의 고정 파일·검증·캐시·취소, 놓친 대상의 임의 교체 방지, 실제 프레임 시각, PC 추적 키와 기존 v4 저장 형식의 호환성을 검사했습니다.
- UI 검사 3개를 추가했습니다. 모자이크/크롭의 서로 다른 모델명·최초 다운로드 동의, PC 처리 동의 전 전송 차단, 다운로드 실패 시 기존 경로 보존, 검토 결과를 적용하기 전 프로젝트 미변경, 설치 안내가 도움말에만 노출되는 동작을 확인했습니다. 이 UI 검사는 가짜 응답을 사용하는 회귀 검사이며 아래 실제 브라우저 실행과 별개입니다.
- 변경한 테스트의 문법 검사와 `git diff --check`를 통과했습니다.

### 실제 PC SAM 2.1 Small 추적

NVIDIA RTX 3060 12GB에서 **640×360, 3초 합성 영상**의 원본 **0.5~2.5초**를 추적했습니다. 기준 시각은 클립 내부 **0.9초**입니다.

| 측정 항목 | 해당 실행 결과 |
| --- | --- |
| 추적 키 / 놓친 위치 | 31개 / 0개 |
| 합성 대상 기준 IoU | 평균 0.9904, 최솟값 0.9848 |
| 실행 시간 | 9.67초 |
| GPU 메모리 관측 표본 | 실행 전 1,088MiB → 실행 중 2,071MiB → 종료 후 1,090MiB |

실제 작업 취소와 프로세스·메모리 정리를 확인했습니다. 프런트엔드에서 로컬 HTTP를 거친 경로도 **모자이크 31개 키, 크롭 32개 키**로 통과했습니다. GPU 수치는 관측 표본이며 최대 요구량을 뜻하지 않습니다. 합성 영상의 IoU·시간은 실제 사람의 가림·급격한 동작이나 긴 영상의 성능 보장이 아닙니다.

### 실제 브라우저 MediaPipe CPU 추론

- 공개 NASA astronaut 시험 이미지에서 **BlazeFace full-range** 얼굴 검출 점수 **0.9261**, **EfficientDet-Lite2** 사람 검출 점수 **0.8761**을 확인했습니다. 점수는 해당 검출의 모델 출력이며 일반적인 정확도 비율이 아닙니다.
- 같은 대상을 이동시킨 **2초 시험 영상**에서 모자이크와 크롭 각각 **실제 프레임 시각의 키 21개, 놓친 위치 0개**를 확인했습니다. 브라우저 CPU 모델의 실행과 대상 연결을 확인한 제한된 사례입니다.

### 실제 원음 분리·믹싱·내보내기

| 입력 / 경로 | 확인 결과 |
| --- | --- |
| 스테레오 MP4 → 별도 원음 PCM | 스테레오 유지, RMS 0.211836. 말소리 믹스도 같은 RMS |
| 위 프로젝트 → WebCodecs MP4 | 720×1280, 65,040 bytes. 재디코딩한 RMS 비율 0.999613, 소리 중복 합산 없음 |
| 오디오 트랙 없는 영상 | 생성한 오디오 클립 0개 |
| 5.1에서 센터 채널에만 소리가 있는 입력 | 다운믹스 RMS 0.149985, 센터 대사 보존 |

스테레오와 센터 채널 시험 두 사례 모두 실제 `.shorts` pack/unpack을 통과했습니다. 분리 원음·원본 출처 표식의 저장/복원과 복원한 PCM 재생 가능 여부도 확인했습니다.

실제 파일 선택창으로 영상을 가져와 타임라인에 넣었을 때 영상과 원음 WAV가 분리됐습니다. 기존 오디오 행이 사용 중이면 **보이스 행을 차지하지 않고 새 오디오 행을 생성**했습니다. 한 번의 실행 취소로 새 영상·원음·추가된 행이 함께 되돌아가는 것을 확인했습니다.

### 실제 VoxCPM2 → Whisper Turbo 연결

VoxCPM2에서 **3.68초 길이, 48kHz PCM, RMS 0.02423**의 음성을 생성하고, 이를 Whisper large-v3-turbo의 **CUDA / int8_float16** 경로로 인식했습니다. 아래 원문과 같은 받아쓰기를 확인했습니다.

> 안녕하세요. 자동 자막 연결을 확인하는 테스트 영상입니다.

기존 등록 참고 음성은 변경하지 않았습니다. 이 한 문장의 생성·인식 성공은 사용자 목소리 유사도나 주관적인 자연스러움, 모든 한국어 발음의 품질 평가를 뜻하지 않습니다.

### 사용자 상태 보존과 검증 한계

- 사용자가 열어 둔 **8791 편집기 탭은 새로고침하지 않았으며**, 초기 임시 초안을 보존했습니다.
- 사용자 래퍼 원본 영상은 제공되지 않아 해당 장면의 추적 품질을 확인하지 못했습니다. 물리적인 모바일 기기, 장시간 영상, 다양한 실제 사람·동물 영상의 통계적 정확도는 미검증입니다.
- 위 회귀 검사와 짧은 실제 실행 결과만으로 새 PC의 설치 성공, 공개 사이트 연결 또는 배포 완료를 주장하지 않습니다.

### 후속 로컬 검증 · 설치·실행기·좁은 화면

- 최종 전체 회귀 검사: **Python 148/148 통과**, **JavaScript 293/293 통과, Canvas 포함 건너뜀 0**. 위의 이전 실행별 총계는 당시 기록으로 유지합니다.
- 실제 `install_pc_support.py --yes --components voice asr tracking` 실행을 통과했습니다. 기존 **설정 5개 파일과 참고 음성 4개 파일의 지문이 전후 완전히 일치**했고, 설치된 **세 모델의 재다운로드는 0회**였습니다. 이는 기존 설치를 재사용한 결과이며 새 PC의 전체 최초 다운로드 검증은 아닙니다.
- Windows `Get-StartApps`에서 일치하는 시작 메뉴 항목이 **1개**임을 확인했습니다. 실제 `.lnk` 실행으로 PC 연결 프로그램이 **PID 9476 → 14112**로 재시작됐고, 직후 HMAC 상태 확인과 세 엔진의 `configured` 상태를 확인했습니다. 사용자의 명시적 선택이 없어 **Windows 자동 시작은 등록하지 않았습니다**.
- 닫힌 Windows 루프백 포트가 `ECONNREFUSED`로 응답하기까지 **2.031초** 걸리는 사례를 관측했습니다. 기존 1초 제한의 종료 상태 오판을 수정해 종료 확인 제한을 **4초**, 설치 프로그램의 종료 요청을 **20초**, 상태 확인을 **2초**로 조정한 뒤 실제 바로가기 실행을 통과했습니다. 이 관측은 해당 PC에서의 사례이며 모든 Windows 응답 시간의 보장이 아닙니다.
- 기존 8791 백엔드는 **PID 20180 → 1228**로 같은 origin에서 업데이트했습니다. 업데이트 후 실제 API에서 Vox의 `ready`, ASR·SAM의 `available: true`를 확인했습니다. 사용 중인 브라우저 탭을 새로고침하지 않았고 초안을 유지했습니다.
- 브라우저 표시 폭 **626.4px**에서 도움말 버튼 하단 **650.4px**가 타임라인 상단 **658.4px**보다 위에 있어 겹치지 않았습니다. 도움말 대화상자가 열리고, 고정 헤더의 닫기 버튼이 보이는 상태(페이지 y **72.925px**)와 **가로 넘침 없음**을 확인했습니다.

이번 좁은 화면 검사는 데스크톱 브라우저에서 수행했습니다. **모델이 없는 새 PC의 전체 최초 설치, 물리적인 휴대폰, 실제 PC 재부팅 후 실행·재연결은 미검증**입니다. 기존 설치가 있는 PC의 공개 설치 파일 실행과 배포 결과는 아래 후속 항목에 기록합니다.

### 후속 공개 배포·설치기 검증

- 추가 보완 후 전체 검사: **JavaScript 299/299, Python 150/150 통과**, Canvas 검사 건너뜀 0.
- 첫 `/pair/start`만 브라우저 권한 선택을 위해 60초 기다리고, 상태·승인 결과 조회는 5초를 유지합니다. 최초 시간 초과와 사용자 취소를 구분하고, 최종 상태 확인 도중 취소하면 연결 토큰을 저장하지 않습니다. 실제 공개 연결 실패의 원인이 시간 초과였다고 단정하지 않습니다.
- Windows PowerShell에서 모듈 자동 로딩 상태에 따라 `Get-FileHash`를 찾지 못하는 실제 설치 중단을 확인했습니다. 파일·ZIP 항목을 공통 .NET SHA-256 함수로 검증하도록 변경하고, 실제 `powershell.exe -NoProfile`에서 해시 동등성·손상 캐시 거부·파일 잠금 해제를 검사했습니다.
- `e624078`의 공개 편집기를 실제 브라우저로 열어 새 도움말·다운로드 링크를 확인했습니다. 설치기와 같은 .NET HTTP 요청으로 받은 CMD **5,625 bytes**, ZIP **131,548 bytes**가 main의 Git blob과 각각 일치했습니다. ZIP SHA-256은 `98404edc81f1bd5841b7732447fd340a5a8e07492b72577ecc5695f951600d73`입니다.
- 위 공개 CMD를 실제 실행해 런타임 준비·해시 검증·ZIP 다운로드·설치·연결 프로그램 시작을 **종료 코드 0**으로 통과했습니다. 기존 세 모델은 모두 재사용됐으며, 기존 설정 **5개**와 참고 음성 **4개**의 지문이 실행 전후 같았습니다. Windows 로그인 자동 실행 질문에는 `n`을 입력했습니다. 기존 런타임과 모델이 있는 PC의 재설치 검증이며, 새 PC의 전체 다운로드 검증은 아닙니다.
- 재설치 후 8792 연결 프로그램과 기존 8791 편집기 API에서 Vox `ready`, Whisper·SAM `available: true`를 확인했습니다. 기존 검증용 편집기 화면에서도 **VoxCPM2 연결됨**, **Whisper GPU (CUDA) · int8_float16 준비됨**을 확인했습니다.
- 공개 편집기에서 PC 연결 요청이 로컬 서버에 도달해 **PC 확인 창의 사용자 승인 대기**까지 진행했습니다. 사용자 승인과 이후 새로고침·브라우저 재시작에 걸친 재연결은 아직 실제 성공으로 기록하지 않습니다.

### 후속 확인 · 아직 완료로 기록하지 않은 범위

- [x] 공개 설치 파일의 이 PC 재설치·실행·기존 파일 보존 확인
- [ ] 모델이 없는 새 PC의 전체 최초 설치
- [ ] 공개 편집기에서 PC 연결 승인·재연결을 수행한 결과 추가
- [x] 공개 배포와 배포본 파일의 main 일치 확인

## 이번 변경 · 타임라인·자막·모션 확장

- Node 5개 테스트 파일 **235개 통과, 실패 0, 건너뜀 0**. 실제 `@napi-rs/canvas`를 사용했습니다. 같은 행 교환과 전환 재연결, 주변 클립 고정, 키프레임·추적의 저장/분할/트림, 300% GainNode와 오프라인 음량 자동화, 폰트·샘플 WAV, 시작/끝 효과·박스/테두리/그림자를 검사했습니다.
- Python 서버 회귀 검사 **53개 통과**. Windows 임시 폴더 권한 때문에 기본 샌드박스 실행은 실패했으며, 승인된 테스트 실행으로 재검사했습니다. 음성 API·PC 추론은 모의 검사이고 이번 변경에서 사용자 녹음이나 GPU 모델을 다시 실행하지 않았습니다.
- JavaScript 앱 모듈 **45개 구문 검사**, `git diff --check` 통과. 프로젝트 글자 스타일의 비유한 수치·문자열 숫자와 HTML 속성 삽입을 거절/정규화하는 회귀 검사도 포함합니다.

### 실제 브라우저 확인

사용 중인 8788 편집기와 개인 초안은 건드리지 않고, main 작업 폴더의 8791 서버와 별도 테스트 탭에서 확인했습니다.

- 기본 샘플의 2번 영상을 1번 자리로 교환했습니다. 전환이 없는 경우와 디졸브로 연결된 경우 모두 3번 영상·자막·그래픽·오디오의 표시 위치가 그대로이고, 전환은 2→1·1→3 연결로 바뀌었습니다. 실제 화면에서는 위치 입력으로 검사하고 포인터 드래그 경로는 이벤트 회귀 테스트로 확인했습니다.
- 보이스 배지 V, 기본 화면에서 자동자막 행 없음, 자막의 표시 구간·트랙 선택·크롭 제거를 확인했습니다. 자동자막 적용·전용 행 생성·재사용은 모의 ASR 결과를 사용하는 테스트로 검사했으며 모델 추론을 다시 실행하지 않았습니다.
- 자막 스타일 카드의 실제 렌더링, 폰트 목록 검색·Bebas Neue 선택, 시작/끝 효과·박스 적용을 확인했습니다. 0초/0.5초 키프레임 추가·값 변경·실행 취소·다시 실행, 새 스우시 샘플 추가와 300% 설정을 확인했습니다.
- 해당 자막·키프레임·샘플 효과음을 **720×1280, 30fps, 2.50초 MP4**로 생성했습니다. 완료 표시 0.1MB, HTML 영상 메타데이터 2.517초/720×1280을 확인했습니다.
- 교환한 영상·재연결된 전환에 위로 밀기 전환과 알림 팝업 그래픽을 적용한 전체 샘플도 **720×1280, 30fps, 11.00초 MP4**로 생성했습니다. 완료 표시 8.9MB, HTML 영상 메타데이터 11.008초/720×1280을 확인했습니다.
- `tests/browser-motion.html` 수동 검사 페이지에서 640×360 H.264 움직이는 무늬 영상을 만들고, 원본 0.5~2.5초를 실제 디코더와 Web Worker로 추적했습니다. **22개 정상 위치, 합성 대상 기준 최대 가로 오차 0.62%**였으며, 추적+위치 키프레임을 포함한 MP4 **2.005초, 172,398 bytes**를 생성해 다시 프레임을 디코딩했습니다. 이는 합성 무늬 시험이며 실제 사람·동물의 정확도를 뜻하지 않습니다.
- 같은 테스트 영상으로 실제 크롭 추적 창을 열고 원본/9:16 미리보기, 시각 이동, 확대 조절, 결과 적용까지 확인했습니다. 검사 페이지의 `public/__qa-motion.html` 복사본은 제거했습니다.

### 남은 확인 범위

실제 스마트폰 조작·메모리·긴 영상, 사용자 촬영본의 사람·동물 추적, 효과음의 주관적 청감, 모든 폰트의 모든 글리프, 여러 코덱을 포함한 브라우저 프로젝트 파일 왕복은 별도입니다. 이번 UI 변경 후 TTS·ASR 모델을 새로 다운로드하거나 개인 녹음으로 추론하지 않았으며 아래 이전 검증을 유지합니다. 출력의 전체 프레임별 시각 비교와 청감 검수도 수행하지 않았습니다.

## 이전 main 통합 · 실행 결과

- Node 회귀 테스트 **152개 통과, 실패 0, 건너뜀 0**. 번들 @napi-rs/canvas 사용. main 통합과 내레이션·기존 화면 호환성 보강 후 재검사 완료.
- Python 서버·PC 보관/인증/실행 경계 테스트 **53개 통과**. 기존 선택형 유료 API 통신 검사는 mock이며 실제 호출 없음. 새 편집기는 이 API를 호출하지 않음.
- JavaScript 앱 모듈 **36개**, Worker와 테스트 문법 검사, Python 서버·실행기·테스트 **14개 파일** 컴파일 검사 통과.
- /studio.html, 기존 코어, 새 자동 편집 모듈과 TTS/ASR Worker HTTP 200 확인. 로컬 서버의 두 엔진 .mjs/.wasm HEAD 200 및 MIME 확인.
- API 키 비노출, 설정 없음, 외부 Origin/Host 거부, 전송 동의와 입력 제한 확인.

## 이전 변경 · main 통합 검증

- 최신 main `ee8e32563022f20dad147bd23d739ec993e32101`을 별도 worktree에서 병합했습니다. 기존 첫 화면·TTS 어댑터·Worker 소스는 해당 커밋과 동일하며, 공유 코어의 내레이션과 새 트랙을 함께 보존했습니다. Worker는 배포하지 않았습니다.
- 기존 화면의 영상 기준 길이, 겹친 자막의 최신 하나 표시, 선택 폰트 로딩과 새 화면의 독립 트랙 동작을 회귀 검사했습니다. 내레이션의 0초 믹싱·보이스 선택·음소거·재생·정지·탐색·끝부분 반복 방지도 검사했습니다.
- 별도 로컬 주소 `127.0.0.1:8791`에서 실제 브라우저로 검수했습니다. 사용 중인 8788 편집기와 개인 프로젝트·참고 녹음은 건드리지 않았습니다.
- 기존 `/index.html`: 공개 샘플 이미지 1장과 한국어 텍스트, 확장 폰트 `베이글 팻`을 추가하고 재생·이전 프레임 이동을 확인했습니다. **1080×1920, 30fps, 3초 MP4 생성 완료(화면 표시 4.2MB)**를 확인했습니다.
- 새 `/studio.html`: 기본 3클립·자막·그래픽·샘플 사운드 프로젝트로 **720×1280, 30fps, 11초 MP4 생성 완료(화면 표시 9.3MB)**를 확인했습니다. 두 화면 모두 이 검수의 브라우저 오류·경고 로그는 없었습니다.
- 위 출력 검수는 생성 완료 상태와 화면 확인이며, 다운로드 파일의 프레임별 비교·청감 평가·실제 스마트폰 검사는 포함하지 않습니다. GPU 엔진 코드는 이번 병합에서 바뀌지 않았으며 아래의 실제 GPU 검증 기록을 유지합니다. 원격 배포 상태는 PR의 GitHub/Pages 결과로 따로 확인합니다.

## 이전 변경 · 실제 Whisper large-v3-turbo GPU 추론

별도 Python 3.11 환경의 faster-whisper **1.2.1**, CTranslate2 **4.8.1**, NVIDIA RTX 3060 12GB에서 `cuda` / `int8_float16`로 실행했습니다. `dropbox-dash/faster-whisper-large-v3-turbo` 모델 revision `0a363e9161cbc7ed1431c9597a8ceaf0c4f78fcf`의 파일 크기와 SHA-256을 설치 때 확인했습니다. 기존 Vox 환경의 Torch·의존성은 변경하지 않았습니다.

이전 Supertonic 2 합성 테스트 WAV만 16kHz 모노 PCM16으로 변환해 로컬 HTTP API로 인식했습니다. 사용자 녹음은 이 검증에 사용하지 않았습니다. 모델은 로컬 파일만 사용하며 추론 중 Python 소켓의 외부 연결·DNS·송신을 차단했습니다. 운영체제 전체의 네트워크 차단을 뜻하지 않습니다.

| 사례 | 실제 결과 | 요청부터 완료까지 |
| --- | --- | --- |
| 4.38초 한국어 편집 안내 | 원문과 같은 문장, 유효한 단어 시각 6개 | 4.375초 |
| 7.92초 날짜·길이·금액 | `2026년 8월 28일`, `15초`, `12,000원`을 포함해 원문과 같은 문장 | 4.187초 |
| 180초로 반복한 합성 음성, GPU 사용 중 취소 | 두 번째 동시 요청 409, 취소 완료·GPU 반환 확인 | 4.641초 |
| 취소 직후 4.38초 음성 재실행 | 정상 완료, 단어 시각 6개 | 4.187초 |

측정에는 매번 로컬 모델을 읽는 준비 시간이 포함됩니다. `nvidia-smi` 표본에서 전체 GPU 사용량은 작업 전 약 6,090MiB, 인식 중 최고 약 7,330MiB, 완료·취소 후 약 6,080~6,090MiB였습니다. **다른 앱과 Vox가 포함된 이 PC의 짧은 사례이며, 최소 VRAM 요구량·실제 녹음의 인식률·일반적인 처리 속도 보장이 아닙니다.**

- 단어 시각과 실제 문장 시각 fallback, 잘못된 시간·장치 결과 거부, 취소 뒤 결과 미적용, 작업 종료 확인 뒤 TTS 예약 해제를 테스트했습니다.
- Windows 작업별 Job에서 자식·후손 종료, 늦은 프로세스 등록 차단, 부모가 갑자기 끝났을 때 워커 종료를 별도 프로세스 테스트로 확인했습니다. 다른 프로그램은 종료하지 않습니다.
- 충분한 GPU 메모리에서는 Vox를 유지하고 부족할 때만 내리는 분기, 다음 요청의 지연 재로딩, 불확실한 종료 시 잠금을 유지하는 동작은 fake torch/모델 회귀 검사로 확인했습니다. 실제 메모리 부족을 강제로 유발하지는 않았습니다.
- 실제 자막 작업 이후 같은 Vox 엔진으로 합성 참고 목소리 TTS를 실행해 **13.032초에 48kHz 모노 PCM16 4.64초**를 받았습니다. 비무음 파형과 새 임시 참고의 삭제 API 성공을 확인했습니다. 기존 사용자 참고 목록·녹음은 읽지 않았습니다.
- 실제 브라우저 검수에서 금액을 분리하던 어절 분할을 발견해 수정하고 다시 GPU로 인식했습니다. `12,000원입니다`가 **6.50~7.44초의 한 자막**으로 보존됨을 확인했습니다. 새 자막 3개는 `자막 2` 트랙에 추가되고 기존 3개 자막은 그대로 유지됐습니다. 한 번의 undo로 새 트랙·자막만 제거되고 redo로 복원됐습니다. Tiny 선택 후 PC 연결 확인을 눌러도 Tiny 선택이 유지되며, 이 검수에서 브라우저 오류·경고 로그는 없었습니다. 원래 사용 중인 8788 탭은 새로고침하거나 편집하지 않았습니다.
- CPU 속도, 실제 스마트폰, 잡음·빠른 발화의 정확도, 이번 변경 후 MP4 내보내기, 공유 사이트 배포는 검증하지 않았습니다. 모바일용 기존 Tiny는 유지합니다.

## 이전 변경 · 실제 VoxCPM2 추론

별도 Python 3.11.13, Torch/torchaudio 2.5.1+cu124, RTX 3060 12GB에서 VoxCPM **2.0.3**을 실행했습니다. 공식 wheel SHA-256 `24da58a30d094a9e9a7ead450ae9cffda0d31eaeba620b61ad99179dd87e486b`, 소스 revision `19b6bf7590025418821a86dcb817504e0ad7e5df`, 모델 revision `32279effe8c19989596f05d353d1447f51d9e915`를 사용했습니다. safetensors·AudioVAE SHA-256 및 허용된 모델 파일 크기를 검증했습니다.

검증에는 Supertonic 2 F1 합성 참고 WAV를 사용했습니다. 실제 사용자 녹음은 생성에 사용하지 않았습니다. 모델 로딩과 생성은 외부 Python 네트워크를 차단한 런타임에서 수행했습니다. 영어/중국어 전용 정규화와 denoiser 없이 요청별 prompt cache로 생성했습니다.

| 사례 | 48kHz 모노 PCM WAV | 요청 소요 |
| --- | --- | --- |
| 한국어 편집 안내 | 4.32초, 414,764 bytes | 첫 요청 42.766초 |
| 베트남·태국 숫자 점수 `4대 2` | 4.16초, 399,404 bytes | 9.431초 |
| 같은 원고의 한글 점수 `사 대 이` | 숫자 입력과 WAV SHA-256까지 동일 | 8.752초 |
| 같은 한국어 안내 1.2배속 | 3.60초, 345,644 bytes | 14.432초 |
| 167자 원고, 두 청크 | 21.88초 | 48.025초 |

숫자 입력과 명시한 한글 읽기의 동일 출력, 1.2배속 길이, 무음이 아닌 PCM, 처리 중 삭제 거부, 임시 참고 삭제와 기존 개인 파일의 바이트 보존을 확인했습니다. 이는 **사람이 듣는 자연스러움·정확한 발음·사용자 음성 유사도 평가가 아닙니다.** 시간은 해당 PC·참고·문장에 한정되며 성능 보장이 아닙니다. 긴 원고는 5분의 HTTP 요청 제한에 도달할 수 있으므로 구간을 나누어 생성하세요.

- provider·모델을 묶은 HMAC 검증, 다른 엔진/변조 응답/구버전 Vox 위장 거부, 인증 전 비밀키·참고 전송 없음.
- 이전 provider 없는 GPT 설정의 해석, 키·원본 설정 백업, 설치/최종 설정 저장 실패 시 기존 활성 설정 보존, 잘못 이름 붙인 엔진 설정 거부.
- 같은 참고 등록을 쓰되 Vox에는 임의 경로·URL·prompt를 전달할 수 없음. 설정·녹음·모델은 Git/ZIP 대상에서 제외.
- 다중 청크 캐시 생성 1회, 중간 실패 시 부분 WAV 미반환, 실제 토큰 기반 출력 제한/최종 실패 재시도 결과 거부.
- 타 엔진이 사용 중인 포트는 종료하지 않으며 Windows Job으로 자체 자식만 정리. 브라우저 원고 입력과 기존 TTS·프로젝트 저장 테스트 유지.

## 이전 검증 · GPT-SoVITS PC 음성 엔진

격리된 Python 3.11.13, Torch/torchaudio 2.5.1+cu124, RTX 3060 12GB에서 GPT-SoVITS v2ProPlus를 실행했습니다. 공식 소스 revision `48b1a0169a28582a8984402f82cf438d3bfa6aca`, 모델 revision `336b2ec4e8d4ac74740798dd40af44e74659ecaf`를 사용했습니다. 다섯 주요 가중치의 SHA-256·크기와 NLTK/fastText 자료의 SHA-256을 확인했습니다.

참고는 이전 검증에서 만든 **Supertonic 2 F1 합성 WAV 4.38초**이며 사용자 녹음이 아닙니다. 편집기 HTTP 어댑터로 참고 등록 → 생성 → WAV 검증 → 참고 삭제를 수행했습니다. 추론 프로세스는 외부 Python 네트워크 접근을 막은 상태였습니다.

| 사례 | 실제 WAV | 생성 요청 소요 |
| --- | --- | --- |
| 한국어 편집 안내 문장 | 32kHz 모노, 4.68초, 299,564 bytes, 무음 아님 | 첫 요청 25.317초 |
| 한국어 날짜·숫자·영문 혼용 문장 | 32kHz 모노, 5.92초, 378,924 bytes, 무음 아님 | 다음 요청 6.463초 |

시간은 이 PC의 두 사례에 한정되며 성능 보장이 아닙니다. 원문 발음·숫자의 정확성, 사용자의 목소리 유사도, 사람이 듣는 자연스러움은 평가하지 않았습니다. 마이크를 통한 실제 사용자 녹음도 아직 받지 않았습니다.

- 실제 엔진의 무인증 문서 접근과 `/control`, 임의 모델 경로 API가 403으로 거부됨을 확인했습니다. `/studio/health`는 비밀키 없이 한정된 nonce의 서명만 반환하며 원고·음성·경로를 노출하지 않습니다. 서명을 확인한 다음에만 비밀키와 생성 요청을 보냅니다.
- Node 검사에서 등록창 drop/paste가 일반 소재 가져오기 전에 차단되고, 늦은 마이크 허가·창 닫기·끊긴 저장 응답·삭제 완료·원고 입력 유지 경로를 확인했습니다.
- 생성 음성의 타임라인 추가와 .shorts 왕복 저장을 검사했으며, 참고 ID·문장·엔진 키가 프로젝트에 포함되지 않습니다. 브라우저 디코더는 테스트 fixture이며 실제 브라우저 전체 조작 검사는 아닙니다.
- Python 검사에서 자체 키 challenge 검증, 원본/위조/재사용 응답 거부, 대리 서버·리다이렉트 차단, 범위·동의·PCM 검증, 실제 처리 잠금, 응답 불확실 상태, 삭제 일부 실패와 Unicode 오류 정리, 설치 리소스 실패 시 설정 게시 방지, 별도 프로세스의 외부 DNS/공개 bind 차단·루프백 통신을 확인했습니다.
- 설치 구성 요소를 실제 다운로드·설치하고 실행했으며, 새 PC에서 CMD 전체를 처음부터 누르는 설치 경험과 CPU 성능은 검증하지 않았습니다.
- 최종 인증 변경 후 엔진을 재실행해 두 생성 경로를 다시 확인했습니다. WAV는 위 사례와 SHA-256까지 같았고, 재실행 후 한국어 요청은 5.955초였습니다. Windows Job 종료 시 런처가 만든 자식·손자까지 종료되는지 별도 실제 프로세스 테스트를 추가했습니다. 등록한 참고 파일은 검증 종료 후 삭제했습니다.

## 다중 선택·직접 편집 회귀 검사

- 기존 역할 없는 v4의 ID·순서·혼합 행·오디오 용도·저장 버전 유지, 행별 바로 위/아래 추가, 마지막 역할 행 삭제 보호.
- 사각 교차와 역방향 드래그, 추가/반전/오래된 선택 ID 제거.
- 설정 붙여넣기의 호환 분기·깊은 복사·원본/문구/시각/모자이크 보존. 일괄 속성은 변경한 호환 값만 적용.
- 묶음 이동의 상대 시각·내부 전환 보존, 외부 연결 분리, 충돌·오래된 상태 거부.
- 연결된 디졸브의 일괄 잔물결 삭제와 한 번의 undo, 재생 막대가 없는 선택 클립 유지.
- 분할/복제의 취소·새 편집·두 번째 자원 생성 실패 시 원본 유지와 임시 자원 해제.
- 행마다 장애물이 다른 묶음 복제의 정렬, 최대 86400초와 부동소수점 경계 저장.
- 회전·반전·비균일 확대의 역좌표·crop·반대 resize 경계 고정·보이는 요소 중심 스냅.
- 네이티브 EventTarget 모의 검사: 마우스 이동 여러 번→commit 한 번, 이종 포인터 무시, Esc/blur/cancel/capture 상실 복원, 원위치 회전의 빈 undo 방지.
- 다중 선택 유지 reveal, 일반 trim의 단일 대상 유지, 룰러 종료 후 추가 seek 방지.
- 모니터 캔버스 종횡비·padding 변경·포인터 좌표, 편집 테두리가 출력 픽셀에 포함되지 않는지 검사.
- 보이스 행이 없는 최대 행수 프로젝트의 생성 음성은 소재함 저장·갱신 후 적용 오류를 안내.

## 이전 릴리스에서 확인한 실제 로컬 AI 추론

고정된 두 ONNX Runtime WASM 엔진과 실제 모델 가중치를 **Node 24.19.0에서 직접 실행**했습니다. 모델 6개의 크기와 SHA-256을 검사했으며, 추론 중 외부 fetch는 차단하고 디스크의 공개 모델만 읽었습니다. 이는 브라우저 Worker·Hugging Face 최초 다운로드·Cache API 검사와 다릅니다.

| 사례 | 실제 결과 |
| --- | --- |
| Supertonic 2 F1, 한국어 인사·편집 안내 | 44.1kHz WAV 4.38057초 생성. 유한 PCM, 무음 아님, 클리핑 없음 |
| Supertonic 2 M1, 날짜·길이·금액 문장 | 44.1kHz WAV 7.91930초 생성. 유한 PCM, 무음 아님, 클리핑 없음 |
| F1 생성 음성 → Whisper Tiny | 원문과 같은 한국어 문장, 유효한 단어 시각 6개 |
| M1 생성 음성 → Whisper Tiny | 단어 시각 9개. 연도·금액·일부 표현 오인식 확인 |

검사 설정은 8단계 TTS, 속도 1.0입니다. 기본 UI는 5단계이며 3/5/8단계를 선택할 수 있습니다. 위 결과는 두 합성 음성 사례의 파형과 인식 결과 검사입니다. **사람이 듣는 자연스러움·발음·실제 녹음의 인식 정확도를 검증했다는 뜻이 아닙니다.** 그래서 생성 음성 미리듣기와 자동자막 교정 단계를 제공합니다.

## 새 자동 편집 회귀 검사

- 모든 채널의 RMS, 반대 위상·교대 채널·고주파 신호 보존, 무음/유음/손상된 PCM, 정확한 선택 길이와 말 앞뒤 여유.
- 자동 컷의 원본 trim·페이드·모자이크 유지, 선택 트랙의 후속 항목만 이동, undo/redo.
- 겹침·전환·잘못된 범위·전체 무음·1,000클립 초과·취소·오래된 결과를 적용 전 거부.
- 실제 프레임 지속시간을 사용한 24fps·15fps·VFR 경계. 실패 구간과 미추적 범위의 내보내기 차단.
- 보정한 두 추적 경로 사이의 미확인 구간과 작은 마스크의 불일치를 성공 경로로 연결하지 않음.
- 텍스처의 1~수 픽셀 이동·저대비·가림·큰 위치 점프, 소스 시각 유지, 깊은 복사·복원.
- 반투명 원본의 세부 무늬 제거, 실패 시 불투명 회색, 변형/블러 배경/분할 전후 공통 렌더 픽셀 일치.
- 미리보기 픽셀과 실제 presentation timestamp를 한 프레임으로 보관.
- 유니코드 TTS 분할, 실제 Whisper 시각 검증·클립 offset 적용·불명확한 시각 제외.
- Worker 성공·취소·시작 실패·전송 오류에서 종료. 원격 Web Speech 보이스 제외.
- 공개 고정 모델 GET만 요청, 파일 잘림 거부, 브라우저 캐시 사용 불가 시 재다운로드 경로.
- 기존 자막 보존/새 트랙 추가/오래된 결과 거부. 음성 파일 삽입 도중 취소 시 소재함 보관과 자동 저장.
- 알려진 오디오 시작/끝 밖의 정상 공백과 내부 디코딩 누락 구분, 3채널 유지, 음소거 원본 분석, 취소.

## 기존 편집기 회귀 검사

- 용도별 트랙 추가·삭제, 레거시 그래픽·자막·음성 트랙 매핑.
- 영상·자막·그래픽이 섞인 트랙 삽입, 다른 트랙으로 이동, 목적지 외 트랙 시각 유지.
- 동시 전환 2개에서 활성 미디어 4개 유지, 전환 가중치는 트랙 안에서만 계산.
- 구형 anchor는 저장된 시각·길이를 유지한 채 해제. 영상 트림·이동·삭제 후 독립성 확인.
- 선택 분할, 24/30/60fps 한 프레임 분할, 이미지 모션·페이드 구간과 undo/redo.
- 선행 공백·중간 공백의 선택 및 삭제, 겹친 구간의 합집합, 오래된 공백 선택 재사용 방지.
- 같은 트랙의 다른 종류 항목도 잔물결 삭제에 포함하고 다른 트랙은 유지.
- 오디오·자막 트림의 원본 및 이웃 경계, 반대쪽 끝 고정.
- 버전 1·2·3을 지원하고 새 저장은 버전 4. 실제 PNG를 포함한 .shorts 저장 왕복에 트랙·변형·크롭 보존, 모자이크의 별도 undo/redo 검사.
- 잘못된 트랙 종류/ID, 비정상 변형·크롭·겹침·시간 정보 거부.
- 세 영상 트랙의 쌓임 순서와 투명한 영역의 아래 레이어 보존.
- 투명 디졸브의 RGBA 보간, 요소 전체에 불투명도 한 번 적용.
- 여러 자막 동시 렌더, 변형·화면 크롭, 잘린 요소의 가로/세로 중심 정렬.
- 분할 전후 렌더링된 페이드 픽셀 일치.
- 말소리를 다른 오디오 트랙으로 옮겨도 자동 자막 믹스 용도 유지.
- 합성 효과음 20종의 WAV 헤더·실제 길이·유한한 샘플·피크·무음 여부·결정성·서로 다른 파형.
- 플랫폼 가이드의 사각형 범위와 미리보기 전용 동작.
- 실제 그리는 자동 문구·폰트 굵기 준비, 필요한 글꼴만 요청, 폰트 URL에 자막 내용 미포함.
- 글꼴 binary 실패 및 취소가 내보내기 준비를 중단함.
- 기존 800회 결정적 편집 회귀 포함.

120개 검사에는 네이티브 이벤트 모의 검사도 포함합니다. 실제 브라우저를 열거나 클릭한 결과는 아닙니다.

네이티브 캔버스에서 공통 renderFrame의 실제 픽셀과 그래픽 **20종**·자막 스타일 **16종**을 검사했습니다. 이 검사는 **실제 브라우저 레이아웃·폰트 다운로드·인코딩된 MP4 검수와는 다릅니다.**

## 초기 실험판 당시 남겨 둔 확인 범위

아래 목록은 초기 실험판의 기록입니다. 이후 실제 브라우저·MP4·GPU 확인 범위는 위의 날짜별 검증을 우선합니다.

- 실제 브라우저 클릭·드래그·키보드 전체 흐름, 작은 화면의 배치와 포인터 캡처.
- 브라우저의 실제 Google Fonts 다운로드와 모든 한국어 글리프 표시.
- IndexedDB 새로고침 복구 및 실제 영상·오디오 코덱을 포함한 프로젝트 왕복.
- MP4/WebM 내보내기와 완성 파일의 화면·음향·자막 싱크.
- 긴 영상·큰 파일·휴대폰 성능·HEVC/MKV 등 코덱 조합.
- 브라우저 Worker의 실제 TTS/ASR 최초 다운로드·캐시 재사용·메모리·취소 및 설치된 운영체제 보이스 재생.
- 실제 사용자 영상의 얼굴·번호판 추적 정확도, 실제 녹음에서의 자동 컷·자막 정확도, 사람이 듣는 TTS 음질.

당시에는 사용자 편집 화면을 변경하거나 새로고침하지 않고 공동 검토 브랜치에서 작업했습니다. 이후 main 통합과 브라우저 검수는 위에 별도로 기록했습니다.

## 다시 실행

```powershell
python -m unittest discover -s tests -p 'test_*.py' -v
node --test tests/core.test.mjs tests/assets.test.mjs tests/motion.test.mjs tests/visual-effects.test.mjs tests/editor-controls.test.mjs
```

네이티브 캔버스 검사는 @napi-rs/canvas가 없으면 건너뜁니다. 설치된 모듈의 절대 경로를 STUDIO_CANVAS_MODULE 환경 변수로 전달하세요. 편집기 실행에는 테스트용 패키지가 필요하지 않습니다.

실제 Worker·인코딩 수동 검사는 `tests/browser-motion.html`을 `public/__qa-motion.html`로 복사하고 로컬 서버에서 해당 페이지의 실행 버튼을 누릅니다. 사용자 소재는 사용하지 않습니다. 검사 후 public 아래 복사본을 삭제하며, 테스트 원본은 tests 폴더에만 보관합니다.
